/**
 * The Jira adapter's id, on its own so the client bundle (the Jira facet in
 * `wiki-filter.ts`) can name it without importing the adapter and its server
 * dependencies.
 */
export const JIRA_TRACKER_ID = "jira";
