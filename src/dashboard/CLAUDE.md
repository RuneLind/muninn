# Dashboard Module — Architecture & Rules

## File Overview

| File | Role |
|---|---|
| `routes.ts` | Creates Hono sub-router, registers all route modules. Also mounts `GET /api/attention` inline — the home "Attention" surface (stale watchers / pending gardener drafts / failed recent runs). Assembly is the pure, injectable `src/dashboard/home-attention.ts` (`assembleAttention`, mirrors the agents/models/indexing assembler pattern); never 5xx (degraded sources → `errors[]`). Stale-watcher detection is `isWatcherStale` — `lastRunAt + 2×intervalMs < now`, but the effective interval is floored at 1 day for **hour-gated** watchers (a short interval is only a floor there; without the floor, e.g. a 5-min-interval daily digest reads permanently stale). The gardener-drafts source is TWO seams: `getDraftCounts` (per-bot `countDraftWikiProposals`) AND `getWikiDraftCounts` (per gardener-capable standalone `WIKI_EXTRA` wiki via `countDraftWikiProposalsByWiki` — the consolidation-gardener's `synthesis` proposals are keyed by `wiki_name`, invisible to the bot-keyed count; surfaced as a `gardener_drafts` item with a `?wiki=<name>` review link). |
| `openapi-spec.ts` | OpenAPI 3.1.0 spec covering all JSON API endpoints |
| `index.ts` | Barrel export: `createDashboardRoutes` |

> **Moved out (2026-06, layering cleanup):** the `activity-log` and `agent-status` singletons now live in `src/observability/` (they are imported by core/scheduler/watchers, not just the dashboard — see `src/observability/CLAUDE.md`). The MCP debug client moved to `src/ai/mcp-tool-caller.ts` (the openai-compat connector consumes it, so it belongs in the lower `ai/` layer). The dashboard still consumes all three; only the source-of-truth directory changed.

## Route Organization

Routes are split by domain in `routes/`:

| File | Endpoints |
|---|---|
| `data-routes.ts` | Main CRUD: messages, memories, goals, tasks, watchers, users, threads, connectors |
| `sse-routes.ts` | SSE streams on `/api/events`: `activity`, `stats`, `agent_status` (phase), `request_progress` (single-pane waterfall), and `agent_runs` (full snapshot of ALL AgentRuns for `/agents` — initial via `agentStatus.getAll()`, live via `subscribeAll`, throttled ~1/s in the tracker, tools capped at 20 per run) |
| `agents-routes.ts` | `/agents` live-agent dashboard page + `GET /api/agents/overview` (JSON: `running` + `upNext` + `recent`). Assembly is the pure, injectable `src/dashboard/agents-overview.ts` (`assembleAgentsOverview`). `running` from the AgentRun registry; `upNext` from `scheduled_tasks.next_run_at` + watchers (next fire mirrors `isScheduledTimeDue` — time-of-day slot dominates a naive `last_run_at + interval`; `computeWatcherNextRun`); `recent` is a four-source UNION keyed by span NAME (chat `%_message`/`telegram_voice` roots + `watcher:%` child spans via `src/db/agent-activity.ts`, extractor `haiku_usage` rows, and the registry completed-runs ring for gardener_drain/capture/research/per-task only — per-kind source-of-truth prevents double-counting). The payload also carries `estimates` — per-live-run `expectedDurationMs` keyed by `estimateIdentity(kind, name)` (`src/dashboard/agent-eta.ts`; watcher identities sourced from durable `watcher:<type>` span durations via `getWatcherRunDurations`, every other kind from the in-memory completed-runs ring, chat NEVER estimated, median of the last ~20) — and `processStartedAt` (the "history resets on restart" empty-state). The SSE `agent_runs` cards carry no estimate; the `/agents` client looks each live run up in `estimates` and computes pace (`elapsed/done × total`) locally where a gardener drain reports discrete progress (pace beats the history median). Never 5xx — degraded sources land in `errors[]` (a degraded `watcher_durations` just drops watcher estimates → elapsed-only). |
| `traces-routes.ts` | Trace spans: list, detail, waterfall data, cleanup |
| `search-routes.ts` | Knowledge base document search and management |
| `research-routes.ts` | `/research` page: cited Q&A over the shelf corpus (`GET /api/research/ask` SSE — retrieve via `researchKnowledge`, synthesize one cited answer; see `src/research/{corpus,answer,ask}.ts`) + the browse `/api/research/*` endpoints. Also the Jira research flow (`/api/research/chat`: trigger analysis, post to chat). |
| `memsearch-routes.ts` | Memory semantic search (hybrid FTS + vector) |
| `graph-routes.ts` | Knowledge graph page + wikilink edge extraction across knowledge collections |
| `wiki-routes.ts` | `/wiki` reader page + `/api/wiki/{pages,page,html,ask,explain,factcheck,digest,atlas}` (per-bot knowledge-wiki browser). **`GET /api/wiki/atlas`** serves the Atlas tab projection (`projectAtlas`); with **`?semantic=1`** and a wiki that has `collections`, it also attaches a `semantic` overlay — huginn per-collection similarity graphs (`top_k=6`, `min_similarity=0.9`) unioned by the pure `joinSemantic` (`src/wiki/atlas-semantic.ts`): `edges` (full-index-resolved, max-sim deduped), namespaced `communities` (`<collection>:<id>` — per-collection Louvain ids restart at 0), `nodeCommunity` (first collection in `collections` order wins overlaps), `nodeType`/`nodeTags` (badge/label info for pages capped out of the rendered columns). Huginn ids match index pages on the NFC-folded `coverageKey` but emit on bare `normalizeRelPath`. Degrade is the contract: huginn unreachable / non-200 / no collections ⇒ no `semantic` field, HTTP 200, payload otherwise untouched; per-collection fetches run concurrently under one ~2.5s deadline; full-success unions cache 5 min per wiki (non-sticky degrade — failures never cache). The client (`views/components/wiki-atlas.ts` + pure helpers in `wiki-atlas-semantic.ts`) renders community dots + a rendered-pill-gated legend with dim-others, dashed semantic edges on selection (threshold slider 0.90–0.995 default 0.98, "+N similar not shown" for capped-out neighbors, toggle default OFF with eager fetch), and the collapsible "Clusters at threshold" rail — client-side union-find over the FULL graph, ≥3-member components, >40-member blob guard, synthesis-candidate badge (≥3 narrative-type members, 0 synthesis-type, via `CLUSTER_ROLE_BY_TYPE`). The per-collection fetch/deadline/TTL-cache orchestration (`getSemanticOverlay`, `fetchSimilarityGraph`) was **extracted to `src/wiki/atlas-semantic.ts`** alongside `joinSemantic` so the consolidation gardener reuses it; the atlas route just calls it (byte-identical behaviour). **Consolidation gardener — Draft synthesis (PR 2):** a `candidate` rail cluster carries a **Draft synthesis** button → `POST /api/wiki/atlas/draft-synthesis` `{wiki, members[], label}`, which NEVER trusts the client badge — it resolves the members in the wiki index, rebuilds a FRESH overlay, and re-runs `computeClusters` (`confirmSynthesisCandidate`, scanning thresholds MAX→MIN) to reconfirm the members are a candidate; a cheap raw `> RAIL_BLOB_MAX` guard rejects oversized payloads early. Dedup on live-OR-applied topic_keys (`getLiveOrAppliedTopicKeysByWiki`, matching the GET's pending-mark so an APPLIED topic also 409s a re-POST) → 409 `{state:"pending"}`; a second draft in flight for the same wiki+topic → 409 `{state:"running"}`; ANY draft in flight for the same wiki (per-wiki single-flight, v1 contract — closes the label-drift double-spend) → 409 `{state:"running"}`; unknown wiki 404; collection-less wiki → clean 400 like Ask. On success the draft runs **DETACHED** (respond `{state:"started"}`, `/agents` visibility via the traced one-shot) through `src/gardener/synthesis-drafter.ts` (`draftAndPersistSynthesis`): harvest bounded member excerpts → saga-style prompt → traced `consolidation:draft` one-shot on the resolved synthesis bot (`resolveWikiSynthesisBot`, 8k thinking cap, never bare `executeOneShot`) → BESPOKE `synthesisShapeGate` (frontmatter `type: blog`, confined under `blogs/`, EVERY member wikilinked — shares only `isPathConfined`/`containBodyLinks` with `draft.ts`) → persist a `synthesis` proposal keyed to the wiki (`wiki_name`, `bot_name` = the truthful synthesis bot, `related_pages` = members for apply-time See-also wiring). The GET atlas response also carries `semantic.pendingSynthesisTopics` (`getLiveOrAppliedTopicKeysByWiki`, attached OUTSIDE the cached overlay) so a cluster whose `synthesisTopicKey(label)` already has a live/applied proposal renders a "proposal pending → gate" link instead of the button. The client rail renders the Draft control from module-level per-topic sets (NOT mutated button DOM), so a threshold-slider / Types↔Months re-render preserves state: `draftingTopics` (a disabled "drafting…" button), `retryTopics` (a "retry draft" button + aria-label, the non-color a11y cue), else the active button. A POST does NOT optimistically mark pending — it starts one shared bounded reconcile poll (5s, ~3min) that re-reads `semantic.pendingSynthesisTopics` and promotes the topic to pending when its proposal persists, or drops it to retry on timeout; the click-time topicKey is re-stamped onto recomputed clusters (`RailCluster.synthesisKey`) so a label drift can't strand the state. `synthesisTopicKey` lives in the import-safe `wiki-atlas-semantic.ts` (client rail + server share it — slug parity). `/api/wiki/explain` (SSE) is **Select-to-Explain**: a sibling of `/api/wiki/ask` that takes a reader-selected passage (`sel`) on a page (`page`) and runs the SAME research pipeline (retrieval over the wiki's `collections` → coverage gate → cited synthesis → `answer_html`) through `streamResearchSSE`, with a per-wiki system prompt carrying the passage's article context (excerpt located in the body via a markup-tolerant locator + the wiki's Similar titles as background). Both markdown and HTML **explainer** pages are supported: an explainer body is reduced to prose via `htmlToText` (`src/wiki/explain-context.ts`) before the same locator runs (explainer selections reach the pill via the injected iframe bridge, below). All prompt/context assembly is the pure `src/wiki/explain-context.ts` (`buildExplainAskOptions`); the preflight decision chain is the exported pure `resolveExplainPreflight` (in `wiki-routes.ts`); the Similar fetch is the shared `fetchSimilarPages` (`src/wiki/similar.ts`, timeout-bounded here so it can't stall the stream open). Same never-5xx contract as Ask (400 JSON for missing `sel`/`page`; wiki/page problems as `app_error`). **Client (PR 2; rail redesign PR 4):** selecting text in a rendered markdown article reveals an "✨ Explain" button in the breadcrumb bar above the article (`wiki-browser.ts` — `renderBreadcrumb` renders it hidden per page, shown only while a selection exists; styled in `wiki-page.ts`. The pre-PR4 floating pill / `positionExplainPill` are gone — the button lives in a stable spot, no page-scroll math); activating it runs the explain stream through the shared `runAskStream` (the Ask runner, extracted so Ask + Explain share supersede/buffer-reset/SSE handling) — which auto-switches the right rail to the **Ask tab** (`switchConnTab("ask")`; the Connections/Ask tab pair, removed in PR 4, was restored afterward because the pinned Ask ate the rail and left Similar below the fold) — so the explanation lands in the article pane + session history like any Ask turn. Pure label/URL helpers live in the import-safe `wiki-explain.ts` (unit-tested). **Fact check (`/api/wiki/factcheck`, SSE):** a sibling of Explain that web-verifies a page's factual claims — selection-scoped (`mode=sel`, reusing the same `renderBreadcrumb` selection state as Explain — a "✓ Fact check" button beside "✨ Explain") or whole-article (`mode=article`, an always-visible "🔎 Fact check" breadcrumb button on markdown pages AND explainers). Unlike Explain it runs **NO retrieval and NO coverage gate** — a dedicated `streamFactcheckSSE` (`routes/factcheck-sse.ts`, sibling of `research-sse.ts`; `streamResearchSSE`/`streamResearchAnswer` are byte-untouched) runs a **THREE-phase** pipeline: (1) **claim extraction** — `callHaikuWithFallback` over the capped article body / the selected passage, parsed by the pure `parseClaimList` (null ⇒ a clean `app_error`), emits a `claims` SSE event `{claims:[{index,title}]}`; (2) **bounded-parallel verification** — an inline `FACTCHECK_CLAIM_CONCURRENCY`(=2) worker pool (`runClaimPool`, pure/injectable/unit-tested) over per-claim tool-enabled `executeOneShot` calls (each 90s `FACTCHECK_CLAIM_TIMEOUT_MS`, R1's one-block output), each emitting a `claim_result` `{index,verdict,markdown}` (verdicts ✅/⚠️/❌/❓); (3) **compose** (multi-claim only) — a small `executeOneShot` (`thinkingMaxTokens:0`, `timeoutMs:COMPOSE_BUDGET_MS`; tools stay available — NOT disabled — a stray tool excursion just burns the compose budget and degrades to the neutral header) writing the overall-assessment lede, whose deltas DO stream via `text_delta`→`delta` (nothing else running); the server assembles `lede + blocks` (`assembleFactcheckAnswer`). **Deadline:** the pool checks `Date.now()` against `deadline = start + FACTCHECK_TIMEOUT_MS − COMPOSE_BUDGET_MS` (and the `clientGone` flag, also set by `stream.onAbort`) BEFORE launching each claim; not-launched claims emit a synthetic ❓ "skipped" block (excluded from the count). For N=1 (a single-fact selection) there is NO compose and NO live verdict deltas — the lone block arrives whole via `claim_result` (accepted tradeoff). **Live tool progress:** each verify call's `onProgress` forwards `tool_start`/`tool_end` as SSE `tool` events (with a `claimIndex`, behind the same `clientGone` write-guard) — text deltas are NOT forwarded during the fan-out (index-less parallel deltas would interleave) — `start` carries a server-computed `{label, detail}` from `getToolProgress` (`src/ai/tool-status.ts` — WebSearch→"Searching the web" + query, WebFetch→"Reading" + `www.`-stripped hostname, tolerant of a clipped URL; a clean label/detail SPLIT so the client doesn't double-parse a composed string), `end` just `{name}`. The client's `tool` listener (fact-check turns only) drives the status line and accumulates deduped WebFetch hostnames into `turn.toolSources` — a persisted "Consulting: host · host" chip row on the turn (round-trips through `wiki-ask-session.ts` as an optional string array). Each chip is an `<a target="_blank" rel="noopener">` (factcheck-v3-sources): the `tool` event also forwards the full `url` (`rawUrlField`), and the client keeps the FIRST full URL seen per host in a `toolSourceUrls` map (host→url) to build the href; hostname stays the dedup key + chip label. The map is **persisted** (round-trips through `wiki-ask-session.ts` as an optional host→url record, shape-validated in `isValidTurn`: a malformed map is dropped but the turn kept), so a rehydrated chip keeps its real deep-link; a pre-PR turn / dropped map degrades to `https://<host>/`. The per-claim verify `Sources:` line is now emitted as markdown links (`[hostname](url)` — the prompt contract in `factcheck-context.ts`) with a belt-and-suspenders pure fallback `linkifySourcesLines` (factcheck-only, in `factcheck-sse.ts`, applied inside `assembleFactcheckAnswer`) that wraps any remaining bare URL on a `Sources:` line — the shared `web-format.ts` deliberately has NO global bare-URL autolinker. Ask/Explain never emit `tool` (only `factcheck-sse.ts` forwards it; `research-sse.ts` is byte-untouched). Corpus-independent, so it needs NO `collections` but DOES need a **web-tools connector** (`connectorCapabilities().supportsWebTools` — `claude-cli`/`claude-sdk` only; a Copilot/OpenAI-compat bot preflights out with a clean `app_error`). Raised `FACTCHECK_TIMEOUT_MS` (280s) and a `FACTCHECK_MAX_CLAIMS` (8) cap sized to the measured ~15s/web-verified-claim. Traced under one `factcheck` root — an `extract` span (Haiku, + `haiku_usage` row via the threaded tracer), one indexed `claude:claim-<i>` span per verify (concurrent `start("claude")` would clobber a shared label; WebFetch tool child spans attached via `attachToolSpans(…, "claude:claim-<i>")` — the optional 4th `parentLabel`), and a `compose` span — and registered as a `factcheck` `/agents` run (`RING_RECENT_KINDS`) with phases `extracting_claims`→`verifying_claims` + per-claim `updateProgress`. All prompt/context assembly (extraction / per-claim verify / compose prompts, prior-block sentinel strip `<!-- factcheck:start/end -->`, `parseClaimList`) is the pure `src/wiki/factcheck-context.ts` (`buildClaimExtractionPrompt`/`buildClaimVerifyPrompt`/`buildComposePrompt`, unit-tested); the route only reduces the body (`htmlToText` for explainers) + computes the base hash, and the SSE builds the prompts at runtime. Client: both buttons call the shared `runAskStream` against the factcheck URL, so verdicts land in the article pane + `askTurns` like any Ask/Explain turn — flagged `kind: "factcheck"` (drives the "Checked N claims against the web" status line + gates the ➕ button), with the `done` payload's `claimCount` (only real verify blocks — synthetic ❓ skips excluded) + `baseHash` (page-content sha256) + `bodyLen` (the pinned `integrateBodyLen` referent, **omitted entirely for explainer pages** — they are HTML on disk and can never be integrated, so an HTML byte count would be a number nothing enforces) + the checked page's `name`/`type` stored on the turn. The `claims` handler seeds a per-turn checklist (`n. ☐ <title>`, transient/not persisted), `claim_result` swaps each row for its verdict block, and a multi-claim turn's compose deltas accumulate into a separate `composeText` lede rebuilt above the blocks (`rebuildFactcheckBuffer`); once total > 1 the claim counter OWNS the status line (per-tool flips suppressed). Pure URL/label helpers `buildFactcheckUrl`/`factcheckLabel` in `wiki-explain.ts`. **➕ Add to article (`POST /api/wiki/factcheck/append`):** a plain JSON route (normal REST semantics — 400 missing `page`/`answer`/`baseHash` · 404 unknown wiki/page · **400 explainer-page reject** (markdown-only — appending a callout to a standalone `.html` would corrupt it) · 409 stale `baseHash` · 200 `{written, page}` — but the whole body is wrapped so an unexpected failure returns 500 JSON, mirroring `POST /api/wiki/remember`) that persists a fact-check answer as a `> [!factcheck]` Obsidian callout (`buildFactcheckBlock`, tone `info` via the render.ts `calloutTone` map — no fifth style) fenced by the `<!-- factcheck:start/end -->` sentinels. Writes via the DEDICATED `appendBlockToPage` helper (`src/wiki/append-block.ts`, unit-tested) — NOT the gardener `applyWikiProposal` path (which would run containBodyLinks + alias-strip over the whole page): confinement (`isPathConfined`) → **raw-bytes** `sha256` staleness vs `baseHash` (same convention as the factcheck route; a mismatch is 409, NOT mtime — an append changes the raw content, so an older turn's baseHash correctly goes stale) → `spliceSentinelBlock` (REPLACE an existing block in place, else insert before a trailing `## Sources`, else append at end) → `Bun.write` → `insertLogEntry` into log.md → refresh the wiki index cache → fire-and-forget huginn reindex over the wiki REGISTRY entry's `collections` (NOT `reindexCollectionFor`, hardcoded to jarvis's wiki/wiki-life; no collections ⇒ skip reindex, still write + log). Allowed on ANY registered wiki's markdown pages. Client: the button renders ONLY on committed fact-check turns whose `pageType !== "explainer"` (hidden entirely for explainer checks), delegated at the document level (the pane re-renders per turn) and gated on the turn being committed like the follow-up/Remember bars; success swaps to a non-interactive "✓ Added to article" + reloads the page in the reader so the callout is visible; 409 shows a "page changed — re-run the fact check" message. **Integrate into article (factcheck v4, PR 1 = backend):** the second write action on a fact-check turn — instead of appending a callout it EDITS THE PROSE in place from a structured edit list. Two plain-JSON routes, same never-throw wrapper as append. `POST /api/wiki/factcheck/integrate` `{wiki, page, answer, baseHash}` **proposes**: baseHash CAS FIRST (409 `{error, stale:true}`), markdown-only (explainers 400), body cap `INTEGRATE_BODY_MAX` (24k) → 400 "page too long to integrate", claim anchors parsed SERVER-SIDE out of the posted `answer` via `correctableClaims` (the `### <emoji> Claim n/m — <title>` heading contract; VS16-tolerant on ALL four emoji, `—`/`–`/`-`/`:` separators, case-insensitive `claim`; ❌/⚠️ only — ✅/❓ untouched in v1). NB the `answer` is CLIENT-POSTED: server-side parsing buys formatting robustness and ONE implementation of the heading contract, **not** trust — a caller that can post an answer can post any verdict blocks, which is why every edit is previewed, bounded and CAS-guarded, then ONE fenced one-shot on `resolveWikiSynthesisBot` (no bot ⇒ 409) returning `{"edits":[{claimIndex, verdict, old, new, reason}], "note"}`. It NEVER writes; the response is `{edits (range-resolved, with `start`/`end`/`tier`/`resolvedText`/`beforeCtx`/`afterCtx` — `resolvedText` is the RAW slice that will actually be replaced, which a tier-2 rescue makes differ from the model's `old`), dropped:[{edit, reason}], note, budget, hasSentinelBlock}` — zero surviving edits is a valid 200. **`hasSentinelBlock`** (PR 2, additive) is the shared `hasFactcheckBlock(body)` (`factcheck-context.ts`) — the PAIRED non-greedy sentinel regex the strip/splice/exclusion-zone authorities already use, NOT a bare `includes(FACTCHECK_SENTINEL_START)` (an orphan START would otherwise default the refresh checkbox ON, make the apply append a SECOND block, and leave the next strip swallowing the prose between them): the client cannot derive it (the page body never reaches the turn), and it is what defaults the reader's "also add summary callout" checkbox ON for a page already carrying a stale callout and OFF for a clean one. It rides BOTH 200 paths, including the zero-claims early return (which precedes bot resolution — hence a model-free route test). `dropped` carries EVERY rejection: the model's own malformed items (`parseEditList`), the count/size caps, the unresolvable anchors, AND the propose-time **ratio budget** (`enforceChangeBudget`, greedy in edit order over the RESOLVED spans) — so accept-all can never be a guaranteed 400 at apply. Reserved basenames (log/index/CLAUDE, either extension) 400 at BOTH routes via `hasForbiddenBasename`, before the one-shot is spent. `POST …/integrate/apply` `{wiki, page, edits, baseHash}` **applies** the client-echoed accepted edits (stateless; apply deliberately trusts the payload — auth-less loopback — but bounds it HARD: ≤`INTEGRATE_MAX_EDITS` 12, ≤`INTEGRATE_MAX_EDIT_CHARS` 2000 per old/new, total changed chars ≤ `max(0.25 × bodyLen, 2000)`; any breach ⇒ 400 NAMING the bound — the changed-chars bound is measured ONLY INSIDE the write section on the freshly-resolved spans (there is deliberately NO cheap pre-resolution ratio gate: the payload sum `Σ max(old,new)` is not a bound in either direction — a tier-2 span can exceed `old.length`, while an `old` carrying reflowed whitespace OVER-counts it and would falsely 400 a set propose approved); `INTEGRATE_BODY_MAX` is re-checked here too, and a path-confinement rejection maps to 400 not 500), returning `{applied, calloutAdded, dropped, page, committed, reason?}`. **Optional `appendCallout` + `answer` (PR 2, additive, paired — `appendCallout` without a non-blank `answer` is a 400):** when set, the SAME `transform` that splices the accepted edits ALSO splices the refreshed `> [!factcheck]` callout (`buildFactcheckBlock` → `spliceSentinelBlock`, replacing an existing block in place) — one write, one CAS, one commit. It must ride this write and not a follow-up POST to the append route, because applying the edits stales the very `baseHash` the client holds. The callout deliberately does NOT rescue a zero-resolving apply into a write: `applied: 0` stays a clean no-op with `calloutAdded: false` (reported on BOTH terminal paths, so "no callout" is never inferred from an absent field), and the ➕ button remains the way to add a callout alone. `coerceClientEdits` is held at parity with `parseEditList` — blank `old` and EMPTY `new` both rejected (an empty `new` is a silent deletion), bare ⚠ normalized, `reason` trimmed, and injected `factcheck:start/end` sentinels neutralized in every `new` at BOTH entry points. **The pure engine is `src/wiki/integrate-edits.ts`** (the test seam): TWO masks over the body, never fused — `matchMaskBody` is SAME-LENGTH (each excluded code unit → one U+E000 `ZONE_SENTINEL`; region removal would splice text into adjacency and let a phantom `old` match across the seam; NUL is forbidden by defensive invariant since `Bun.spawn` rejects it in argv) and drives all offset math, while `promptMaskBody` collapses each zone to a readable placeholder for the model. Zones = frontmatter · `factcheck:start/end` blocks · fenced code (``` AND ~~~, marker-matched with a CommonMark closer-length rule (the closing run must be ≥ the opener's, so a ``` line inside a ````-opened block is content, not a closer), and scanned only OUTSIDE the frontmatter/sentinel zones so a stray ``` quoted inside a persisted fact-check block can't invert fence parity for the rest of the page; INDENTED code blocks are deliberately left editable) · (on `.mdx`) **BLOCK**-component TAG MARKUP ONLY, line-anchored and derived from markdown-ast's shared `COMPONENT_TAG_SOURCE_SINGLE_LINE` — the newline-free attribute-tail variant, so a tag missing its `>` can't swallow the prose below it up to the next `>` anywhere in the page; the leading-indent allowance is deliberate since `tryParseComponent` matches the TRIMMED line (inner prose stays editable; INLINE tags and prose mentions of a component are deliberately NOT masked — masking them made every sentence containing one permanently unintegrable with a misleading "no longer found" reason). `integrateBodyLen` is the ONE pinned body-length referent — the factcheck `done` SSE payload's new additive `bodyLen` and the integrate route's cap check both call it, so a code-heavy page can't have the client budget against a number the server never enforces. `applyEdits` resolves all ranges against the ORIGINAL masked body (exact unique match, else a tier-2 whitespace-COLLAPSED retry mapped back through `collapseWithMap` — now exported from `explain-context.ts`; 0 or ≥2 matches ⇒ dropped, never fuzzy-applied). The tier-2 rescue is GATED by `collapsedRescueRisk`: `collapseWithMap` also strips `*`/`_`/backtick and rewrites `[label](url)` → `label`, so a mapped-back range can start after an opening delimiter while consuming the closing one (`**NEW`), eat a link's URL (`See [NEW here.`) or span a paragraph break — a rescued range whose raw slice contains a paragraph break (`\n[ \t\r]*\n` — a bare `\n\n` test misses CRLF pages), whose delimiter counts differ from `old`'s, or whose EDGE sits offset inside markup (the count check alone is invariant under a ONE-DELIMITER SHIFT: `[Claude 3](url-a) before GPT-4o` maps back to a slice with identical counts that splices in the OTHER link's URL — so each edge is boundary-tested against the body char just outside it) is REJECTED with an honest reason rather than spliced, rejects overlaps (earlier wins), then splices DESCENDING by start offset — no `String.replace` and deliberately no per-edit re-validation mid-apply. `parseEditList` validates to null (a malformed response ⇒ 502, never a write) and reports per-ITEM failures as `dropped` entries rather than `continue`ing them into the void (#397's silent-drop class). The answer-block parser lives in the import-safe `views/components/wiki-integrate.ts` (the `synthesisTopicKey` precedent — server + PR 2's client share ONE implementation; tolerant of an optional VS16 on all four emoji, of `—`/`–`/`-`/`:`, and of a lowercase `claim`; the `###` anchor is deliberately NOT loosened). The one-shot runs through `src/wiki/integrate-oneshot.ts` — its own `factcheck-integrate` trace root + `factcheck` `/agents` run, `tracedOneShot` for the child span, 8k thinking cap, 90s timeout, and the bot config CLONED with `FENCED_EXCLUDED_TOOLS` unioned on (muninn #397: under `bypassPermissions` `allowedTools: []` fences NOTHING — an unfenced model would just `Edit` the page and bypass the preview + CAS + queue; the fence does NOT bind on `openai-compat`, which ignores `excludedTools`). That whole sequence — trace root + `/agents` run + tool fence + 8k thinking cap + finish/complete in try/catch — is the shared `runFencedOneShot` (`src/core/fenced-one-shot.ts`), which the source-page drafter (`runDrafterOneShot`) also calls; `DRAFTER_EXCLUDED_TOOLS`/`DRAFTER_THINKING_MAX_TOKENS` remain as compat re-exports. **Write plumbing (shared with append):** `appendBlockToPage`'s whole sequence was extracted to `src/wiki/writeWikiPage` (`src/wiki/page-write.ts`) — confinement → read → CAS → `transform` → write → log.md, all INSIDE a per-**wiki-root** queue (`src/wiki/queue.ts`, keyed on the root's **realpath** via `wikiWriteQueueKey` so two registry paths for one wiki — symlink, `/tmp`→`/private/tmp` — can't get independent chains; `log.md` is wiki-GLOBAL, so a page-keyed queue would interleave two writers and lose an entry, and a queue entered after the CAS compare closes no race). The queue serializes the append + integrate writers against **each other** — it is not a global log.md lock: the gardener apply path writes log.md under its own per-bot mutex, and the refresh/reindex/commit tail runs outside the section (both pre-existing), then refresh → fire-and-forget reindex → commit. `transform` returning `null` short-circuits to a `noop` outcome (zero edits resolved ⇒ no write, no log, no reindex, no commit). The COMMIT is awaited AFTER the section releases and its `CommitWikiResult` rides the outcome: `commitWikiChange` enqueues on the per-git-toplevel COMMIT chain, so a shared chain map + an in-section await would self-deadlock on a wiki whose root IS its git toplevel — the two queues therefore own separate `Map`s (`createQueue`) AND the commit sits outside. `append-block.ts` is now a thin splice + those strings — behaviour-preserving apart from the new per-wiki write serialization, plus an optional `commit` on the `AppendOutcome` (`append-block.test.ts` passes unchanged as the regression gate). **Client (PR 2):** a "✎ Integrate into article" bar sits under the ➕ bar on a fact-check turn, with the transient preview panel below it. Every gate and HTML builder is pure + unit-tested in `views/components/wiki-integrate.ts` (which now also OWNS the `INTEGRATE_BODY_MAX` declaration, re-exported by `integrate-edits.ts` — the bundled client must not import the server module, which would drag `explain-context.ts` → `research/answer.ts` into the browser bundle): `integrateBarState` returns `hidden`/`pending`/`ready`/`too-long`/`done`/`blocked-append`, where the ≥1-❌/⚠️ test runs the SHARED `correctableClaims` heading parser over the persisted answer (a ⚠️ in the compose lede or inside a ✅ claim's reasoning must not false-positive a substring scan), an over-cap `turn.bodyLen` renders the page-too-long state CLIENT-side (~10% of mimir's pages exceed 24k — don't spend a round-trip learning it from a 400), and an ABSENT `bodyLen` (a pre-`bodyLen` turn) deliberately renders the button and lets the server's 400 drive it. The preview diffs `edit.resolvedText` — the RAW slice the server will replace — falling back to `edit.old` only when absent, since a tier-2 rescue makes them differ; a `collapsed` tier renders a chip, `beforeCtx`/`afterCtx` render dim, `dropped` renders as a collapsed "N not applied" list, and the LCS renderer is the gardener's dependency-free `src/gardener/diff.ts` `lineDiff` with the same `d-add`/`d-del`/`d-ctx` classes. `beforeCtx`/`afterCtx` are CLAMPED at exclusion-zone boundaries server-side, so frontmatter / fence content / a persisted callout is never shown as adjacent prose. **All panel state is state-driven, never DOM-held** (`IntegratePreviewState`): the preview is TURN-KEYED (`renderIntegratePreview` paints only when its turn is the one on screen — a ~90s propose resolving after a turn switch neither strands nor paints a live Apply under an unrelated turn; re-opening the turn from history brings it back), the in-flight apply lives on `applying` (a mid-flight checkbox toggle re-rendered a fresh ENABLED Apply and detached the captured error nodes), and `applyBlocked`/`applyDropped`/`droppedOpen`/`applyDroppedOpen`/`focusEditIdx` survive the wholesale re-render every toggle causes. The propose-time and apply-time dropped lists carry **separate** open-state fields even though both render as `.wiki-fc-int-dropped` — the capture-phase `toggle` listener tells them apart by the apply-time list's `apply-drops` modifier, since one shared field let each list's toggle clobber the other's disclosure state (apply-time defaults OPEN: an `applied: 0` must name its reasons without a second click). **`refreshWriteActionBars` is TURN-GUARDED** (`turn !== askShownTurn` ⇒ early return) for the same reason `renderIntegratePreview` is: the ➕/✎ bars are singleton nodes owned by whichever turn is painted, and the six late callers (4 SSE handlers, `setIntegrateBarMsg`, the propose `finally`) would otherwise paint turn A's live ✎ button — or its 409 copy — into turn B's retired bar, reviving a button whose click fires a doomed ~90s one-shot against the wrong page. Nothing is lost by skipping: `showAskAnswer` re-renders both bars from the newly shown turn and every bit of bar state is held off the DOM (`turn.wrote`, `integrateBarMsgs`, `integratedNotes`, `integrateProposing`). An `applied: 0` renders the route's per-edit `dropped` reasons and disables re-Apply until the selection changes. The in-flight PROPOSE is likewise derived (`integrateProposing`), so the two write buttons are deliberately NOT in `setFollowupDisabled` — every former caller re-derives via `refreshWriteActionBars` instead of blanket-enabling a button the derivation had retired. The callout checkbox label branches on `hasSentinelBlock` (refresh-in-place vs add) and renders DISABLED when the turn carries no answer; `integrateSuccessCopy` consumes `calloutAdded` (added/refreshed, or an explicit "was NOT added" when it was requested). Both write outcomes are ALSO written to the rail's Ask status line, the only surface that survives the `loadPage` reload (which replaces `#articleWrap`, bars included). The apply route normalizes BOTH branches through `withTrailingNewline` so the checkbox can't change unrelated trailing bytes, and a client-posted `answer` is capped at `FACTCHECK_ANSWER_MAX` (32k) at BOTH write routes. Accept disables on an empty selection or when the selected `Σ max(resolvedText, new)` exceeds the echoed `budget.maxChangedChars` — UX only; the server re-measures the freshly-resolved spans and owns the real 400. **Persisted wrote-flag:** `StoredAskTurn` gained `wrote?: "append"|"integrate"` + `bodyLen?: number` (both optional-additive, validated in `isValidTurn`, round-trip-tested). BOTH write buttons' disabled state is DERIVED from `turn.wrote` at render time and each write calls `persistAskSession()` immediately — the session only persists on explicit calls, so a DOM-only disable would come back live after a reload against a `baseHash` the other write already staled (either write ⇒ the other shows the same copy a live 409 would). The preview state itself is per-turn TRANSIENT (module-level `integratePreview`, cleared by `showAskAnswer`); a rehydrated turn just re-proposes on click, since the persisted answer + baseHash are the whole propose input. Both bars are re-rendered from the turn at `done` (`refreshWriteActionBars`) because the pane was painted before the answer existed. Buttons + checkboxes are delegated at the document level (`click` and `change`) like the ➕/follow-up bars. **Inline `<Fact>` annotation (fact-check v5, PR 3 = the write path):** the integrate write is now **strip → resolve → splice**. Both propose and apply run the pure `stripFactWrappers` (`src/format/markdown-ast.ts`) over the body FIRST — offsets then agree with what the model was shown, and a re-run cannot nest a wrapper inside a wrapper; claim extraction (`factcheck-sse.ts`) and the selection locator strip too (the locator's pre-pass is inside `locateExcerpt`, deliberately NOT inside `collapseWithMap`, whose discarded-delimiter counts are exactly what `collapsedRescueRisk` gates on). `annotateEdits` (`src/wiki/integrate-edits.ts`) turns the model's corrections plus the persisted answer's claims plus PR 2's `{index, quote}` pairs into ONE ordinary edit list: a ❌/⚠️ claim's FIRST accepted edit gets its `new` wrapped (`<Fact n v>` + edit.new + `</Fact>`; later edits for the same claim apply unwrapped, so there is always exactly one chip per claim), a ✅ claim's quote becomes a WRAPPER-ONLY edit over its resolved span (as does a ⚠️ claim's, when pass 1 left it unmarked — a flagged passage whose correction dropped must still carry a visible mark; a claim already wrapped by pass 1 is excluded, so there is never a second chip with the same `n`), a correction naming a `claimIndex` NOT in the answer applies unwrapped (a chip for it would link to a section the appendix cannot contain), and ❓ claims get neither a mark nor an appendix section — they ride the `<FactCheck unknown=…>` count instead. **Two passes, corrections first**, because `applyEdits` rejects overlaps by BODY POSITION: a ✅ wrapper starting earlier would otherwise silently kill an overlapping ❌ correction, so corrections resolve and CLAIM their spans and any intersecting wrapper is dropped ("overlaps a correction"). The **wrapper-only predicate** is structural and server-derived, never a payload flag — `edit.new` must equal one of the two legal spellings (`factWrapperForms`) around the span the edit ACTUALLY resolved to — and it drives all three budget authorities (`outcomeChangedChars` at propose, the in-transform apply re-measure, and the client's `editChangedChars` mirror), where a wrapper-only edit costs **0**: marking a long ✅ sentence would otherwise book ~1.2k chars for a write that alters no prose. **Newline guard** (`factSpanForm`, wrapper-specific, pre-splice — the inline component matcher is line-scoped): tier 1 newline-free span ⇒ inline form; tier 2 exactly ONE whole paragraph ⇒ block form (tags on their own lines); tier 3 partial-paragraph multi-line ⇒ trimmed to the longest newline-free run with any leading list/quote marker left OUTSIDE the mark; else dropped with a reason. A correction whose wrapper can't be built applies UNWRAPPED (never dropped) — except when the POST-wrapper `new` breaks `INTEGRATE_MAX_EDIT_CHARS`, which would hard-400 the apply. **Caps:** on an annotatable page the edit cap is `FACTCHECK_MAX_CLAIMS + INTEGRATE_MAX_EDITS` (20) at BOTH routes — so the apply route's count check now runs AFTER `resolveIntegrateTarget` (which reads the index, not the body); propose trims the excess, and corrections are ordered first so the cap can only ever trim MARKS. **Appendix:** `buildFactcheckAppendix` emits the `<FactCheck>` component form, isMdx-GATED via `isAnnotatablePage` at BOTH call sites (`.md` keeps the `> [!factcheck]` blockquote — it is read raw on GitHub). It is UNCONDITIONAL on any apply that writes a wrapper (chips link to `#fc-claim-N` sections only it provides), so the checkbox is replaced by a statement in the preview, `buildIntegrateApplyBody` posts `answer` whenever `carriesFactWrapper(selected)`, and the route 400s a wrapper-carrying apply with no answer via a fail-closed `/^<Fact\b/` PAYLOAD-shape pre-check (deliberately distinct from the post-resolution structural predicate, which needs a body the route hasn't read yet). The transform is deterministic: drop the compose lede and everything outside a claim block · order by SEVERITY (❌, ⚠️, ✅) then claim index · drop ❓ (counted in `unknown=`) · tag-neutralize the CLIENT-POSTED answer via `COMPONENT_TAG_SOURCE` exactly as sentinels are neutralized (a line trimming to `</FactCheck>` would close the block early and strand every claim after it) · one `Was: <original>` paragraph immediately before the mandatory `Confidence:` line, from `originalsOfOutcomes` over THIS write's freshly-resolved spans (integrate-only — the ➕ append route changes no prose and can never produce one). Zero counts are OMITTED, never `0`. **All-✅ gate relaxation, `.mdx` only:** the three ❌/⚠️-gates (the button via `hasIntegrableClaims`, the propose early-return, the e2e) relax to "≥1 parsed claim PLUS ≥1 claim quote" when the turn is `annotatable` (the annotate-only path's marks have no anchor but the quotes, so a quote-less all-✅ turn renders the `no-anchors` bar state instead of a button whose only outcome is an empty panel), and propose then skips the editor one-shot entirely. The **supersede rule** is the strip: prior marks not re-emitted by this run are reported as a run-level `supersededNote` on the proposal (`countFactWrappers`, rendered beside `quotesNote`), never silently vanished and never as a blank row in `dropped` that inflated the "N not applied" count. Preview groups the wrapper-only edits into ONE checkbox (`annotationIndexes` + the annotation card) instead of N no-op diffs. Acceptance is committed as tests: the appendix is asserted BYTE-EXACT against the approved fixture's block (`factcheck-appendix.test.ts`), wrapper placement is asserted structurally against a pinned real-extraction triple (`factcheck-annotate-acceptance.test.ts` + `__fixtures__/factcheck-creatine-*`), and `integrate-annotate.test.ts` covers the tiers, the two-pass overlap, the caps and re-run idempotency. Smoked end-to-end in `e2e/wiki-integrate.spec.ts`, which boots its OWN muninn on port 3021 with `WIKI_EXTRA` pointed at a temp wiki, stubs ONLY the propose call (`page.route`) and lets the real apply write, asserting the corrected sentence is in the reloaded article and the pre-edit sentence is gone (plus the `not-a-repo` "not committed" copy, the all-✅ no-button gate, and the post-append derived disable). **Explainer selections (PR 2):** standalone HTML explainers render in a sandboxed (opaque-origin) `<iframe sandbox="allow-scripts allow-popups">` — no `allow-same-origin`, so the parent can't read the frame's selection. `/api/wiki/html` **appends** a listener-only forwarder script (`EXPLAINER_BRIDGE_SCRIPT` in `src/wiki/explainer-bridge.ts`, injected by trailing-append — no anchor parsing) that `postMessage`s the selected text + nearest heading + selection rect up to the parent (`wiki-explain-sel` / `wiki-explain-clear`, `targetOrigin: "*"`). The parent's one module-load `message` listener (`wiki-browser.ts`) trusts a message ONLY when the current page is that explainer AND `event.source` is the live frame's `contentWindow` (looked up per event), then reveals the same breadcrumb Explain button (pre-PR4: translated the frame-relative rect into the viewport and showed a floating pill). Sandbox attributes are unchanged — the script adds no capability beyond reading the iframe's own selection. `positionExplainPill` takes a **viewport-relative** rect (md caller derives it from its Range; iframe caller offsets by the frame's `getBoundingClientRect()`) and is the ONLY place page scroll is added. Scroll-inside-the-iframe dismissal is v1-cosmetic (parent gets no scroll event; the pill moves off only on selection change / Escape / click-away). **Page recency** rides on `mtimeMs` (file mtime, stamped on every page by `src/wiki/store.ts` — not just explainers)

**Native `.mdx` pages (first-class markdown):** `src/wiki/store.ts` discovers `**/*.{md,mdx,html}`. A `.mdx` file is treated exactly like a `.md` page — frontmatter (title/tags/type), `[[wikilinks]]` + relative-link graph membership (wikilinks inside component bodies count), backlinks, and index-coverage (`isIndexableId` accepts `.mdx`; it counts as markdown, not `htmlPages`). It renders **inline** through `renderWikiHtml` → `formatWebHtml` (shared block AST, so `<Callout>`/`<Verdict>`/etc. render as components), never in the explainer iframe (its type is never `explainer`). Same-stem collisions resolve by precedence `.md` > `.mdx` > `.html` — the loser is dropped from the index (logged at warn), the read-side counterpart to mimir's authoring-side `scripts/mdx-explainer/checks.ts` guard. **Compile-pipeline SOURCE exclusion (load-bearing):** because the collision key is the BARE stem across all folders, a mimir MDX source at `blogs/src/<slug>.mdx` would otherwise SHADOW its own compiled explainer `blogs/<slug>.html` (`.mdx` rank 1 beats `.html` rank 2), silently dropping the explainer the reader is meant to serve and stripping its backlinks. So the store excludes a `.mdx` from discovery entirely when it is a pipeline source — detected surgically (`pipelineSourceSiblingHtml`) by the documented layout (immediate parent dir `src`) **reinforced by** a compiled sibling `<dir>/<stem>.html` actually existing and carrying the generated marker (`<!-- generated from`). A stray/hand-authored `.mdx` — even one under a `src/` dir with no compiled twin — stays a first-class native page; the exclusion is logged at DEBUG (expected pipeline shape), not the WARN used for real shadow collisions. muninn has no mermaid renderer, so a ```mermaid fence in a native page renders as a plain code block (compile such diagram pages to `.html` explainers via the mimir MDX pipeline instead). `src/wiki/similar.ts` strips component tags before building the semantic-search query so JSX-ish tokens don't pollute the embedding.: the reader's "Recently updated" sort key is `pageTimeMs` = the NEWEST of the frontmatter `updated`/`created` date, the **git touch date** (`gitTouchedMs`), and **mtime — but only when the file is dirty**. Frontmatter-less wikis (mimir, melosys-kode-wiki) rank honestly instead of sinking to the bottom as undated, and a sweep no longer flattens the whole list. **`gitTouchedMs` is the newest NON-SWEEP commit** to touch the page, rename-aware (`src/wiki/git-dates.ts`): a commit touching ≥ `SWEEP_THRESHOLD` (10, flat — parity with mimir's own `scripts/plan-status/git-touch.ts`) files inside the wiki subtree is a mechanical pass whose timestamp describes the sweep, not the page, and contributes NO update date at all. mimir's 2026-07-31 backfill made all 148 plans read as edited that minute (truthful and useless); measured over mimir's 495 commits, 32 classify as sweeps and the largest plans/ bucket goes **147 → 18**. **The mtime rule is the crux:** mtime cannot simply be dropped (a page edited right now has no commit and mtime is its only evidence) but it is also exactly the corrupted value, so it is trusted **iff `git status` reports the file dirty** (`gitDirty`, from the same `listWikiSubtreeDirty` the Index card uses) — precisely the case history doesn't know about yet. **`gitCreatedMs`'s presence is the discriminator**: absent ⇒ git knows nothing about this page (non-git wiki, failed walk, or an untracked draft) ⇒ mtime is trusted unconditionally, i.e. byte-identical pre-PR behavior; present ⇒ it also serves as a FLOOR, so a page whose every commit was a sweep (19 of mimir's 151 plans) falls back to its **creation** date rather than sorting as undated. That coupling is safe because both maps come from one walk and `created` is a superset of `touched` by construction. Sort key and label come from one `updatedSignal` so the list always shows the date it sorted on. **The article header shows BOTH dates** (`pageHeaderDates`) — `created X · updated Y` — while a LIST ROW keeps showing the single date it sorted on. A one-slot header whose word flipped between "added" and "updated" shipped first and was too subtle to read; two labelled slots say the whole thing. Rules: no KNOWN edit (`pageDateKind` = `added`) ⇒ the creation date ALONE — deliberately not derived by comparing the two labels, since a truer frontmatter `created` can be OLDER than the git floor the update signal fell back to, so they'd differ and the header would invent an edit; created and last-edited the same day ⇒ one slot; otherwise both. Measured on mimir: 178 pages show both, 209 one. The sibling "Recently added" sort key is `pageAddedMs` = the OLDEST of three signals — the frontmatter `created` date, the **git creation date** (`gitCreatedMs`), and the file birthtime (`birthtimeMs`; ≤0 ⇒ undefined). Oldest-wins is the mirror of `pageTimeMs`'s max and is principled: every way a creation date gets corrupted moves it FORWARD, so the oldest surviving claim is the most trustworthy. Deliberately NOT a trustworthiness ranking — `git mv` resets birthtime while git remembers the rename, and a page hand-imported from another repo has a truer `created:` than git's import date; neither dominates, so the min picks whichever still remembers. **`gitCreatedMs` exists because birthtime is not a creation date in a git-managed wiki** (`src/wiki/git-dates.ts`): `git mv` (mimir's 2026-07-08 `wiki/`→`projects/` reorg birthtimed 302 files that day), a re-clone, and any sweep writing via temp-file+rename (mimir's 2026-07-31 plan-status backfill: 148 files, all birthtimed 12:31 — an in-place overwrite preserves birthtime, temp+rename resets it) all reset it wholesale, and mimir declares no `created:` (1 of 154 plans), so the sort collapsed onto one day ordered by title. ONE `git log --reverse --name-status -M --diff-merges=first-parent --format=%at` per index build (~135–290 ms on mimir/jarvis full history, 5 s budget, kicked off so it overlaps the ~700-file read pass) yields BOTH date maps — the same `--name-status` output carries a per-commit timestamp AND that commit's file list, so the sweep classification needs no second subprocess and no second traversal (`git status` for `gitDirty` is a separate cheap probe, run concurrently). Scoped by pathspec to the wiki subtree so a wiki inside a bigger repo (jarvis's `huginn-jarvis/data/wiki`) stays cheap — and that scoping is also what makes the sweep count mean "files inside the wiki subtree" for free. **First appearance wins and a rename CARRIES the source's date** — without the rename rule every renamed page would date to the rename commit, which is the same bug relocated. The root is realpath'd before subtracting the git toplevel (`--show-toplevel` reports a symlink-resolved path, so on macOS every `/tmp` wiki — the e2e `WIKI_EXTRA` fixtures included — otherwise produced a `..` pathspec and degraded the whole walk to null; same rule as `wikiWriteQueueKey`). Not a git repo / no history / timeout ⇒ null and the field is simply absent, i.e. exactly the pre-existing frontmatter+birthtime behavior; an untracked page likewise keeps its honest birthtime. The one case that warns is an unexpected **RATE**: pages carrying no git history AND not dirty (`git status` lists untracked files, so a genuinely new page has an innocent explanation) exceeding `GIT_DATE_MISS_WARN_RATE` = 10%. Deliberately not the "0 hits" guard PR A shipped — that was blind by construction to the bug it was written for, since the `core.quotePath` regression dropped 32% of huginn-nav while the ASCII majority kept the count above zero. Healthy state measured at exactly 0 unexplained on all four wikis. **Future-date guard (ignore, never clamp):** EVERY branch of BOTH signals — frontmatter, git dates, mtime/birthtime, all inside their `consider()` gate — ignores a stamp more than `FUTURE_DATE_SKEW_MS` (48h — timezone/clock skew, nothing more) ahead of now, because `consider()` accepts any positive ms and a model-emitted `updated: 2027-01-01` would outrank every real signal FOREVER; frontmatter is the likeliest offender (it is authored, not observed) but git's `%at` is the settable AUTHOR date and an mtime can be stamped ahead by a bad clock, so calling those two "machine timestamps of events that actually happened" and exempting them was wrong — under `addedSignal`'s MIN the extra coverage only bites a page whose EVERY signal is implausible; it is **ignored, not clamped to now** (which would invent a "last edited today" the page never earned), and the page falls back to its git/mtime evidence with the label following the key so the row still shows what it sorted on. Valid past dates are untouched, so no existing ordering moves. Two details are load-bearing: (a) **the fallback re-tries `created`** — the chain used to resolve `p.updated || p.created` BEFORE the plausibility test, so one bad `updated:` also discarded a perfectly valid `created:` and dropped the page to the git floor or to undated (`authoredUpdatedDate`; an UNPARSEABLE `updated:` deliberately still does NOT fall through — that is the linter's `stale-updated` case, not the sort's); (b) **`now` is a parameter, not a `Date.now()` read inside the signal** — these run inside sort comparators, where a per-call clock read makes the comparator impure for a page sitting exactly at the boundary, so `sortPages`/`pageHeaderDates` capture one instant per pass and thread it through; (c) **that instant is SERVER-ANCHORED, not the raw viewer clock** — `wiki-filter.ts` is bundled into the browser (`wiki-browser.ts` is a `Bun.build` IIFE entrypoint), so a bare `Date.now()` is the VIEWER's clock and a machine running >48h SLOW would judge every legitimate frontmatter stamp in the wiki implausible at once, silently collapsing the whole listing onto its git floors. `wiki-browser.ts` therefore derives `recencyNow()` = `anchorNow(Date.now(), scannedAt)` from the `/api/wiki/pages` payload's `scannedAt` and threads it into `sortPages`/`pageDateLabel`/`pageAddedLabel`/`pageHeaderDates`. `Math.max` is the only safe direction: `scannedAt` rescues a slow clock, a FAST clock keeps its larger value (which only makes the guard MORE permissive), and it is deliberately not a REPLACEMENT since `scannedAt` can be a full index-TTL old. A null/absent `scannedAt` degrades to the bare clock. Because the sort's whole job here is to drop the bad stamp SILENTLY, the operator-visible signal is the **linter**: `checkStaleUpdated` (`src/wiki/lint.ts`) reports an `updated:`/`created:` past the same `FUTURE_DATE_SKEW_MS` under the existing `stale-updated` key (importing the exported predicate — one constant, one implementation, the shape `store.ts` already uses for `sanitizeColorToken`), so it surfaces on `/wiki/gardener` + the weekly `wiki-linter` alert. A **bulk-restamp cohort detector** (`src/wiki/restamp-detect.ts`, a build-time warn on N pages sharing a stamped day their git history contradicts) was written and REMOVED before merge after being executed against the 5 real wikis: 3 of its 4 live cohorts were genuine bulk content edits whose stamps are truthful (verified by diff), the only metadata-only cohort was mimir's HTML explainers, whose `updated` is mtime-synthesized pseudo-frontmatter (`buildExplainerMeta`) that regenerates on every clone — so it was structurally unfixable, nondeterministic across builds, and accruing a new permanent warn-cohort every 1–2 weeks. Timeline/Atlas/article-head rendering of raw frontmatter is deliberately left untouched (authored-content display; zero future stamps exist in real data today, and the lint now surfaces any that appear). Sort key and label come from one `addedSignal` so the list always shows the date it sorted on (frontmatter echoed verbatim, git/birthtime rendered as LOCAL days — the same split `updatedSignal` makes). Same fallback feeds the Timeline tab (frontmatter-less pages file under their mtime date) and the picker's freshness label. The left pane also carries a derived **folder facet** (`pageFolder` = the first path segment, `ROOT_FOLDER` = `"/"` for wiki-root pages) — a picker built from the pages themselves (never hardcoded), hidden on a flat wiki, reset when a domain switch filters its selection away. Both live in the pure `views/components/wiki-filter.ts`. `/api/wiki/html` serves standalone `.html` explainers (type `explainer`, indexed by the store) as raw `text/html` for the reader's iframe — resolved strictly via the index entry's stored path, verified under the wiki root — with the Select-to-Explain forwarder script appended (see the explainer-selections note above; full-text read + append, stream-free). `/api/wiki/ask` (SSE) is the reader's **Ask** tab: research-style cited Q&A scoped to the selected wiki's `collections` (bot `wikiCollections` / `WIKI_EXTRA` 3rd segment), delegating to `streamResearchAnswer` and enriching citations with matched wiki page names (`src/wiki/citation-links.ts`) so they open in-reader; a collection-less/unknown wiki emits a clean `app_error`. **Owner-routing:** the synthesis bot is `resolveWikiSynthesisBot(entry, discoverAllBots())` — an explicit per-wiki pin (`wikiSynthesisBot` config field / `WIKI_EXTRA` 4th segment) wins first (bypassing the opus fast-gate); else the owning bot answers its own wiki (jarvis wiki → jarvis, nav → melosys); standalone or opus-owned wikis fall back to the research bot. The `/wiki` page resolves the same bot at render time and shows an "Answered by <bot> (connector · model) — owner/fallback" line under the Ask tab's hint (`askBot` option on `renderWikiPage`). The Ask route also passes a per-wiki `systemPrompt` (`buildSynthesisSystemPrompt(...)` framed to the wiki name) through the shared `research-sse.ts` helper; `/research` leaves it unset (byte-identical LC prompt). Switching the synthesis bot also switches the retrieval identity (decomposer backend + `research_citations`/`search_signals` attribution). The right-column rail is a **Connections | Ask tab pair** (restored after PR 4's pinned-Ask experiment left Similar below the fold): the **Connections** tab (default on load) holds Linked from / Links to / Similar; the **Ask** tab holds the compose controls (question box · status line · clickable session-history list) and owns the full rail height when active. `switchConnTab(tab, focus?)` toggles them; the rail **auto-switches to Ask** whenever an Ask/Explain stream starts (`runAskStream` calls it — covers the Ask box, the in-pane follow-up bar, and Explain), switching back to Connections is manual. The Ask **answer renders in the main article pane** as a formatted article. A **coverage footer** under the left page list ("N pages · indexed X · Y missing") links the full Index card, and the left filter stack is a compact head (search + domain segmented control + sort) with a **Filters** disclosure holding the type/tag/folder chips (auto-opened whenever a filter is active). **Plan-status facet:** the disclosure also carries a **Status** chip row + a `⚑ has follow-ups` toggle, gated on `statusFacetVisible(allPages)` — ≥1 page carrying a `plan_status` OR ≥1 with `followups: open`, since the two axes are independent and a followups-only wiki would otherwise render ⚑ flags with no legend and no toggle — so a wiki that uses NEITHER (and mimir mid-backfill) renders exactly as before. That whole-wiki gate is one level above the SCOPED one: `renderStatusChips` hides the row again when the current domain/type scope leaves it holding nothing but the inert "All status" chip and no status/followups filter is active. Conversely the ACTIVE status is unioned into the chip list even at count 0 (in `STATUS_ORDER` position; the tag row's `shown.unshift(filters.tag)` precedent), so a scope switch can't delete the very filter that is emptying the list. Presence IS validity: `parsePlanFields` (`src/wiki/store.ts`) drops any value failing the enum before the payload, so the client never needs its own validator. `status` and `followups` are INDEPENDENT `WikiFilters` axes that AND together ("shipped but with loose ends" is a real query), with an absent `followups` folded to `none` once in `pageFollowups`. `statusCounts`/`followupCount` honor domain + type (the `tagCounts` scoping rule — each row counts within the selection the rows above it made) and deliberately NOT each other, so the ⚑ toggle can't silently rewrite the status numbers. Per-page pills ride BOTH the list rows and the article header from one `statusPillHtml` spelling, colored by a dedicated `plan-<status>` class family (never the `badge-<type>` palette — two independent axes, and `status-*` is taken on /serena + /summaries); the follow-up ⚑ is a separate marker emitted AFTER the pill on both surfaces (list rows and header shipped flipped and were unified), and both are page-driven, so a status-less page shows no pill even where the facet is live. The chip's 6px `.wiki-status-dot` takes the SATURATED `--status-*`/`--accent` tokens via `.wiki-status-dot.plan-<status>` (the `.wiki-type-dot` palette), not the pill's pale `--tint-*` fills — those were near-invisible at dot size while an untinted status kept a plain grey dot, inverting the salience. `status_note` reaches the client UNESCAPED and is escaped into the header pill's `title=`. Pure layer + gate in `views/components/wiki-filter.ts`; the two-wiki gate smoke is `e2e/wiki-status-facet.spec.ts`. After the terminal `done`, the route emits a trailing `answer_html` event — the final answer markdown rendered through the shared wiki pipeline (`renderAskAnswerHtml` in `src/wiki/ask-render.ts` → `formatWebHtml`) with in-range `[n]` markers linkified to their matched pages. The client renders the streaming buffer progressively through the same `formatWebHtml` markdown pipeline (throttled to one animation frame per burst; the import-safe helper lives in `views/components/wiki-ask-render.ts` and is unit-tested), so headings/lists/code grow formatted during the stream, then swaps in the final rendered HTML on `answer_html` (falls back to the progressively-formatted text if it never arrives). The final-render hook is opt-in on the shared `research-sse.ts` helper (`renderAnswerHtml`); only the wiki route enables it (`/research` renders client-side). **Follow-up bar + session persistence (client-only, PR A):** every Ask/Explain answer article renders a **follow-up bar** under its Sources (`#wikiFollowupInput` + `#wikiFollowupBtn`, `wiki-browser.ts`, styled in `wiki-page.ts`) — Enter or the button runs the SAME `/api/wiki/ask` stream via the shared `runAskStream`, so the follow-up lands in `askTurns` (history + `history` context) like any Ask; no server change. Bound via **document-level delegation** (the pane is re-rendered per turn, killing direct listeners) and gated on the turn being committed: rendered disabled, explicitly re-enabled by id in the `done`/`answer_html` handlers (`runAskStream` owns the disable/enable, extended to toggle both the Ask button and the follow-up controls). Committed turns are persisted to `localStorage` under `wikiAskSession:<wiki>` (or `:__default__` on bare `/wiki`) — last 10 turns, quota-retry at 5 then silent, full `AskTurn` incl. `html` so a rehydrated turn re-shows identically via the history-click path. Rehydrated at boot into the "This session" list (no auto-show); a `clear` affordance on the list header wipes the key + `askTurns`. Serialize/validate is the pure, DOM-free `views/components/wiki-ask-session.ts` (unit-tested — round-trip, cap, malformed-JSON/shape drop). **Remember this (PR B):** `POST /api/wiki/remember` `{wiki?, bot?, question, answer}` is the first dashboard-originated memory write (auth-less loopback like `POST /feedback`). A **plain JSON** route (normal REST semantics — 400 missing question/answer · 404 unknown wiki · 409 no-synthesis-bot / no-mapping · 502 distill-failed · 200 `{saved, summary}`), NOT the SSE never-5xx contract — but the whole body is wrapped so an unexpected failure logs + returns 500 JSON, never an unhandled throw. **Attribution rule:** the memory is owned by the wiki's synthesis bot (`resolveWikiSynthesisBot`, same as Ask) and that bot's `bot_default_user` mapping (`getBotDefaultUser`) — Remember requires the resolved bot AND a mapping; either absent ⇒ a clean 409 (never a silent fallback user / orphaned memory). Distill runs through the one-shot Haiku router (`callHaikuWithFallback`, no MCP) with `buildDistillPrompt`/`parseDistillResult` (pure `src/wiki/remember.ts`, unit-tested); a null parse ⇒ 502 (no verbatim fallback). Saved via `saveMemory` (scope `personal`, embedding of the summary, degrades to no-embedding + warn), tags = `["wiki-note", <wiki>, ...distilled]` — the stable `wiki-note` first tag is PR C's injection filter. Activity-logged (`source: "wiki-remember"`). **Saved-notes injection (PR C, server-only):** on the happy path (preflight clean + synthesis bot resolved), both `/api/wiki/ask` and `/api/wiki/explain` do a `wiki-note`-tag-scoped hybrid memory lookup (`searchMemoriesHybrid(userId, question, embedding, 5, botName, ["wiki-note"])` — C0 added the optional `tags` overlap filter) for the wiki's synthesis bot + its `bot_default_user`, and append a `buildSavedNotesBlock` block (pure, `src/wiki/remember.ts`) to the system prompt as **background, not a citable source** (cap 5, wiki-only via the tag filter — general chat memories never enter). Degrade-to-absent: `fetchSavedNotes`/`fetchSavedNotesBlock` (injectable, unit-tested in `wiki-routes.test.ts`) never reject, and the lookup is raced against the shared `PRESTREAM_TIMEOUT_MS` (=3s, also bounds the Explain Similar fetch) ⇒ `[]`/null ⇒ prompt unchanged, so a slow/dead DB can't stall the SSE open. **Null-embedding guard (load-bearing):** `fetchSavedNotes` bails to `[]` when `generateEmbedding` returns null — `searchMemoriesHybrid` would otherwise fall back to an UNFILTERED FTS search (no tags param) and inject chat memories under the saved-notes label. No `bot_default_user` mapping ⇒ injection skipped silently. In Explain the notes lookup runs CONCURRENTLY with the Similar race (`Promise.all` of two raced never-rejecting promises, notes query = `buildExplainQuestion(sel, meta.title)` derived pre-race) so the pre-stream budget stays ~3s, not additive. No new trace spans (pre-stream lookup). Client: a **Remember this** button under the follow-up bar on every Ask/Explain answer article (`wiki-browser.ts`, styled in `wiki-page.ts`) — gated on the turn being committed (same commit gate as the follow-up bar) and delegated (the pane re-renders per turn); success swaps the button to a non-interactive "✓ Remembered: <summary>" line (one save per render). **Continue in chat (`POST /api/wiki/ask/chat`):** the Ask turn's escalation into a REAL conversation thread — plain JSON route with the same never-throw wrapper as Remember (400 missing question/answer · 404 unknown wiki · 400 standalone wiki `{needsBot, bots[]}` · 400 `{needsUser, users[]}` · 409 `{threadExists, existingThreadId}` · 200 `{threadId, conversationId, chatUrl}`), mirroring `POST /api/research/chat`'s plumbing (thread → web conversation → `setPendingMessage` → `/chat?bot=&thread=&user=` deep link, consumed once by the chat page's `handleDeepLink`) rather than refactoring it. **Bot routing is the deliberate divergence from Ask:** the thread goes to the wiki's OWNING bot (`entry.source === "bot"`), NOT `resolveWikiSynthesisBot` — synthesis optimizes for a fast model and falls back to the research bot, but the conversation must land where the user's memories/goals/tools live; a body `bot` overrides (the legacy wiki alias stays the `?bot=` QUERY param). An ownerless standalone wiki falls back to its **synthesis-bot pin** (`entry.synthesisBot` — the `WIKI_EXTRA` 4th segment, validated against discovered bots: on this install mimir→jarvis and melosys-kode-wiki→melosys, i.e. without it the button was dead on 2 of the 3 registered wikis); only a wiki with neither owner nor pin 400s `{needsBot}` rather than silently picking one. User resolution is explicit `userId` → the bot's `bot_default_user` mapping (the reader has no user picker, and a real install carries several historical user rows — without this the button is a permanent `needsUser` 400) → the sole user → `needsUser`. Thread name + seed are the pure `src/wiki/ask-chat.ts` (`deriveAskThreadTitle` — lowercase/flattened/≤50 per `createThread`, truncated surrogate-safely so an astral question can't store a U+FFFD or overshoot the unit budget; `uniqueAskThreadTitle` for the `forceNew` retry; `buildAskChatSeed`). **The 6k seed cap is a bound on the WHOLE message, not just the answer:** every variable part carries a sub-budget (question 1500, sources line 500, and the answer keeps ≥2000 whenever it is non-empty — the question's budget shrinks first), each cut is marked, and a final clamp holds the total. Before that only the answer was budgeted, so a 200k-char question rode along untruncated (205k-char seed) while a ~5.7k question zeroed the answer's budget and produced a seed carrying "(answer truncated)" and NONE of the answer. **`forceNew` walks disambiguators:** the timestamp suffix is minute-precision and `createThread` is ON CONFLICT DO UPDATE, so a colliding name upserted and returned the EXISTING thread — whose unsent seed `setPendingMessage` then clobbered (measured: three forceNew posts in one minute → one threadId). The route now appends `-2`, `-3`… until `findThreadByName` reports the name free (bounded at 50, room taken out of the base). **Order of operations is load-bearing:** all body validation (`wiki`/`bot`/`userId` string-or-absent, citation names string-or-absent — a `{"pageName": 123}` used to reach `flatten`'s `.replace`) and the seed build happen BEFORE `createThread`, so no failure path can leave an orphan thread in the sidebar for a call the caller was told had failed. The web-conversation shell is the DETERMINISTIC `chatState.findOrCreateBotConversation` (id = `deterministicId("<user>:<bot>:web")`), not a random-UUID `createConversation` — a random shell is invisible to later off-band broadcasters addressing `botConversationId` (dev_run roll-ups, hivemind replies), which would then never render in the tab this route just opened. NB `research-routes.ts` still has that bug; it was deliberately left alone. Every side-effecting seam is injectable (`AskChatDeps` / `__setAskChatDepsForTest`) so the route tests run with no DB. Client: a `.wiki-chatesc` bar under Remember on every committed Ask/Explain turn — NOT fact-check turns (synthetic question + tool-produced answer would make the seed's "answered from indexed page excerpts alone — no memories, no tools" line false), and no bar at all until the turn commits (the wrapper is always rendered so `done` can fill it in place; `.wiki-chatesc:empty` collapses it). Own bar — the Remember bar's innerHTML is swapped wholesale on save — document-delegated, and its state is **TURN-KEYED** (`turn.chatEsc`, in-memory only, never in `StoredAskTurn`/localStorage) rather than DOM-held: `#wikiChatEscBar` is a singleton node owned by whichever turn is painted, so a fetch resolving after a turn switch used to paint turn A's success link onto turn B's bar and, on the error path, write into a detached node where the user saw NO error. `refreshChatEscalateBar` is turn-guarded like `refreshWriteActionBars` (which calls it, so every SSE terminal path re-derives it) and the button is deliberately out of `setFollowupDisabled` — that force-enabled it on turns that never committed, where the click was a silent no-op. A **409 is not auto-retried** with `forceNew` (that minted a new thread + a new auto-sent model turn on every re-click): the bar offers the existing thread ("Open it →", built client-side from the 409 body's `existingThreadId`/`userId`/`botName`) with "Start new thread" as an explicit second choice. The tab is **pre-opened synchronously** in the click handler (`window.open("", "_blank")`, `location.href` set on success, closed on failure) because Safari blocks a `window.open` issued after an await unconditionally; a blocked popup renders honest copy ("Chat thread created — open it →") instead of claiming it opened one. The posted answer is capped at 32k, mirroring `FACTCHECK_ANSWER_MAX`. **Direct mode + options popover (PR A):** bot/user resolution was extracted to the shared `resolveAskChatTarget(entry, wantedBot, deps)` — a discriminated `{ok:true, botConfig, bots, users, defaultUserId}` | `{ok:false, reason:"unknown_bot"|"bot_gone"|"needs_bot", bots}` whose three reasons map 1:1 onto the POST's three existing 400 bodies (the POST spells the messages, so no test contract moved). It resolves `bot_default_user` **unconditionally** (concurrently with `loadChatConfig`) but returns it as `defaultUserId` only when the mapped id is a MEMBER of `users` — a stale mapping can't regress the sole-user path, which never consulted the mapping. That unconditional lookup is **caught and degraded to `null`** (warn), because making it unconditional otherwise handed every sole-user escalation — and every GET — a brand-new 500 path on a query neither needs. Each failure's message is the shared `askChatTargetErrorMessage(reason, wiki, wantedBot)`, so the POST's 400 body and the GET's `error` can't drift (they already had). **`GET /api/wiki/chat-target?wiki=&bot=`** is the popover's single prefill fetch — ok path `{botName, users, defaultUserId, preferredForUserId, preferredConnectorId, connectors:[{id,name,connectorType,supportsWebTools}], connectorsError?, botDefault}`, failure path `{botName:null, reason, error, bots}` and NOTHING else (`reason` is the client's only branch key; the `needsBot` boolean re-encoded it, `wiki` was never read, `bots` is meaningless once a bot resolved, and the four dummy filler fields are gone — the client type makes the ok-path fields optional and tolerates their absence). `preferredConnectorId` folds in the user+bot sidebar preference the popover used to fetch in a second round-trip, stamped with `preferredForUserId` (the mapping's user, else a sole user) so a client landing on a REMEMBERED other user still refetches rather than applying someone else's model; the connector listing is `listConnectorOptions` (id/name/connector_type only — `SELECT *` dragged `base_url` through a reader-facing route) and runs concurrently with the bot-keyed resolution — it exists because the reader client holds only `window.__WIKI_NAME__` and the chat bot is a server question (wiki name ≠ bot name; the Ask-answer bot can differ from the chat bot); `?bot=` re-targets everything bot-keyed. Connector capability is flagged **server-side** via the additive `capabilitiesForConnectorType` (`src/ai/one-shot.ts`, which `connectorCapabilities` now delegates to) — the browser cannot run it, and a second hardcoded type list is exactly the drift that makes a picker promise web search a bot doesn't have; a dead connectors table degrades to `connectors: []` + `connectorsError`, never a lost popover. POST gained `mode:"direct"` (the EXPLICIT discriminator that makes `answer` optional — a missing answer alone still 400s), switching to the new **`buildDirectChatSeed`** (`src/wiki/ask-chat.ts`; no quoted answer, same `ASK_CHAT_SEED_MAX` bound). Its framing carries two promises that must be true: it names web search **only when the effective connector has it** (the chosen row's `connectorType`, else the bot's `config.connector ?? claude-cli`), and it orders the bot to "search the wiki's own notes first" **only when the wiki has `collections`** (`hasCollections`; a `WIKI_EXTRA` entry without the 3rd segment has nothing behind Ask, and the order produced answers that opened by apologizing for a search that could never work). The opening is a bracketed provenance line (`[Question asked from the "<wiki>" wiki reader]`), **deliberately not first-person** — "I'm reading the … wiki" got a real `memories` row written asserting a biographical fact about a reader who had only clicked a button. (`buildAskChatSeed`'s "I asked the … wiki" opening has the same disease and predates this; not changed here.), plus `connectorId` (isValidUuid → 400 BEFORE `getConnector`'s uuid-column WHERE can 500; unknown → 400; passed through the widened `AskChatDeps.createThread` seam), `threadName` (same flatten/lowercase/≤50 pipeline as derived names) and `existingThreadId` ("Send there →": validated via the new `getThreadById` seam as belonging to the resolved user+bot, seeds without creating, and **refuses to clobber a live pending seed** — `setPendingMessage` is last-write-wins on a 5-min TTL, so a 409 `{alreadyQueued}` (which carries the thread's `chatUrl`, the reader's only recovery: opening it delivers that seed) beats silently deleting an unopened question. The refusal is TWO checks, not one: a cheap `hasPendingMessage` fail-fast so the common case costs no connector stamp and no conversation shell, plus the atomic `setPendingMessageIfAbsent` at the write itself — the peek and the write are separated by three awaits, which is a real TOCTOU on exactly the question the peek exists to protect. On reuse, a chosen connector is applied via `updateThreadConnector` ONLY when the thread carries none, and when it is NOT applied the 200 says so (`connectorApplied:false` + `keptConnectorId`) — the drop used to be silent and the client wrote the ignored pick to localStorage as honored. The reuse seed's capability likewise comes from the THREAD's own `connectorId` (resolved through `getConnector`; a deleted row falls back to the bot default), because that connector wins at processing time — a copilot-pinned thread was observed being seeded with a promise of web search.) **The chatUrl carries `&src=wiki` — but only when the request MADE a connector decision** (direct mode, or any POST from the options popover, which always sends the `connectorId` key with `""` meaning "(bot default)"; the plain one-click escalation omits the key and keeps the sidebar stamping it has always had). Flagging every escalation silently cost that path its Phase-1 behavior. The same scoping rides the `threadExists` 409's new `chatUrl`, which the client now consumes instead of re-deriving a link that hardcoded the flag. Why the flag at all: choosing "(bot default)" means NO connector on the thread, and the chat page's `stampConnectorOnThread(…, onlyIfEmpty)` only protects threads that ALREADY have one — so a connector-less (or not-yet-loaded) thread got stamped with the sidebar's remembered preference, silently changing which model answered. The suppression therefore lives INSIDE `stampConnectorOnThread` (`src/chat/views/components/connector-selector.ts`), not at a call site: `handleDeepLink` stamps and `sendMessage` stamps again right before delivering the seed, so gating only the first leaves the seeded turn stamped anyway. It gates on `onlyIfEmpty` (the two preference paths) so an explicit dropdown pick still wins, and is keyed **per THREAD, never released for the page session** — a single boolean released after the seeded send protected exactly one message, and the reader's SECOND message in the same connector-less thread was stamped after all. Accepted limitation (stated in the code): a fresh tab on that thread, without the deep link, is unprotected — a durable fix needs the threads table to be able to say "bot default is the choice here", which the absence of a connector cannot express. Client: a **New chat** button beside the Ask box (direct) and a **⚙** on the escalate bar (escalate) open ONE popover (`wiki-browser.ts`, state-driven, document-delegated, tab pre-opened synchronously in the click for Safari); its defaults, labels, capability resolution and lowercased thread-name preview are the pure `views/components/wiki-chat-target.ts` (which imports `deriveAskThreadTitle`/`deriveAskThreadTitleOrNull` from the server's dependency-free `ask-chat.ts` so the preview is byte-identical to the created name — the `…OrNull` split is what lets BOTH sides fall back to the question for a typed name that flattens to nothing, instead of pinning the thread to the generic `wiki ask`). Popover invariants worth keeping: the click-away decision is the pure `shouldCloseChatOptions`, which treats a DETACHED target as not-outside (a submit re-renders the panel synchronously before its first await, so the Send button's own click read as a click-away, closed the panel, and made the in-flight submit close the pre-opened tab — thread and seed created, nothing shown, retry hitting `alreadyQueued`) and never dismisses while `sending` or while the reader is typing in `#wikiAskInput` in direct mode; the question is RE-READ at submit time (the panel snapshots it at open, and typing in the Ask box repaints the name preview and the Send button in place); editing the thread name repaints the FOOT too, since it clears a name collision and a stale "Send there →" posts `existingThreadId: undefined`, silently creating a new thread; the bot picker is rendered from sticky client state (`needsBotPicker` + a cached `bots`), so a resolved pick can still be changed and a FAILED re-fetch doesn't dead-end; `connectorsError` renders as a note under the Model row; and "(bot default)" is remembered as an explicit `__default__` sentinel (storing `""` was indistinguishable from storing nothing, so the chat preference silently re-selected a named model the reader had moved off). Connector default order: `muninn-wiki-chat-connector-<wiki>` → the user+bot `chat_preferences` connector → bot default; user default: `muninn-chat-user-<bot>` (the chat page's OWN key) → `defaultUserId` → sole user, never positional. The stamp regression is tested by evaluating the REAL injected script plus the REAL deep-link/send block lifted out of `chat/views/page.ts` (`chat/views/components/connector-selector.test.ts`) — both failing conditions named: thread absent from the client's `threads` array, and thread present but connector-less. **Ask-decline hook (PR B):** a turn the wiki DECLINED to answer renders a prominent "Ask in chat instead →" action IN PLACE of the ordinary escalate bar, opening the same direct-mode popover with the question **PINNED to the turn**. Deliberately NOT a branch on the incoming event: the `done` flags fire once while the bar is re-derived from turn state on every switch/rehydrate, so the reason lands on `AskTurn.declined` / `StoredAskTurn.declined` (`"no_hits" | "low_confidence"` — persisted, and validated in `isValidTurn` as the two-value union **forward-tolerantly**: an unknown value drops the FIELD and keeps the turn, the `annotatable`/`claimQuotes` treatment rather than `wrote`'s reject-the-turn, which exists only because `wrote` gates a destructive write against a staled baseHash). The mapping is the shared `askDeclineReason` (`wiki-ask-session.ts`), which checks **`lowConfidence` FIRST**: `src/research/ask.ts` sets `noHits: true` unconditionally on BOTH decline branches (it means "nothing was synthesized", not "zero documents"), so a `noHits ? … : …` order mislabels every low-confidence decline — the one case where weak sources DID ride out and are listed under the answer. The same function is injected BY SOURCE into `/research`'s inline client (`research-page.ts`, which has no bundler and cannot import), so that order has exactly one implementation. The markup + copy are the pure `declineChatBarHtml`/`DECLINE_CHAT_BTN_ID` in `wiki-chat-target.ts` (the id is shared by the render, the click delegation and the click-away `inOpener` test) — as is the whole escalate bar now (`chatEscBarHtml`, moved out of `wiki-browser.ts`, which holds only the wiring). **Four ways the hook diverges from the plain "New chat" opener,** each a fix over the first cut: (a) **pinned, not typed into `#wikiAskInput`** — writing the question into the box destroyed the reader's own draft, left the failed question armed there afterwards, and on the Connections tab wrote into a hidden textarea; `ChatOptState.pinnedQuestion` holds it, the pure `chatOptQuestion(state, boxValue)` resolves pinned → escalate's turn question → the live box, and the box is neither read at open nor re-read at submit (the un-pinned "New chat" path keeps PR A's M9 live-box behaviour, and `shouldCloseChatOptions`' "clicking the Ask box doesn't dismiss" rule now applies only there — hence the new `pinned` field on `ChatOptClickContext`). (b) **the pinned question is COMPOSED** by the pure `composeDeclineQuestion`: an Explain turn's `question` is only the display label `Explain: "<80-char slice>…"` (the real question is built server-side from `sel` and never comes back) and a follow-up's is a bare fragment, so both escalated unanswerably — they now go as `About the wiki page "<page>": explain this passage — "<selection>"` and `Context — the earlier question in this wiki session was "<origin>". Follow-up: <text>`, fed by two additive persisted fields (`explainPage`, stamped in `activateExplain` from the page title; `originQuestion`, stamped by `submitFollowup`, chains keeping the ROOT). `explainSelectionFromLabel` (`wiki-explain.ts`) owns taking the label back apart, beside the builder. (c) **the POST carries `askDeclined: true`** so `buildDirectChatSeed` stops ordering the wiki-notes search that just failed (route contract in `src/wiki/CLAUDE.md`). (d) **the turn rides on `ChatOptState.turn`**, so a success mirrors `chatEsc = {status:"done", chatUrl}` onto it exactly as the one-click path does — an Escape or a blocked popup used to lose the created thread's only link. That is also why `chatEscBarHtml` checks `chatEsc` **before** `declined`: a realised escalation outranks an offer to start one, and the earlier order silently shadowed the link while the second click walked 409 recovery for an unreachable thread. Rationale for keeping both entry points: the decline hook catches the honest failures, the always-visible "New chat" button covers the confident whiffs. **Article mode — "💬 Discuss" (PR C):** a third opener for the SAME popover, a breadcrumb button rendered beside the always-visible "🔎 Fact check" article action (`discussArticleBtnHtml`, id shared by the render, the click delegation and the click-away `inOpener` test — the `DECLINE_CHAT_BTN_ID` precedent; deliberately the same row, since both are article-level actions and splitting them makes neither findable). Route contract (`mode: "article"`, server-side page re-resolution, page-derived seed, page-derived thread name, per-mode thread description): `src/wiki/CLAUDE.md`. Client specifics: the question is **EDITABLE, not pinned** — it is typed into the panel's OWN `<textarea>` (`CHAT_OPT_QUESTION_ID`), and `chatOptQuestion` neither reads nor writes `#wikiAskInput` in this mode (article mode has nothing to do with the Ask tab; a click in that box is an ordinary outside click, as under a pinned question). **The question box is ALWAYS empty on open** — behind `ARTICLE_QUESTION_PLACEHOLDER` — and BOTH page summaries ride along as a dim clamped HINT under it (`articleChatHint` = `description || desc`, sliced at `ARTICLE_HINT_MAX` = 240 + ellipsis). The authored frontmatter `description` used to prefill, which is exactly the failure the `desc` demotion was written for: a description is a declarative sentence (blog subtitles, sniffed `<meta>` descriptions — 98 mimir pages carry one), Send is ENABLED on whatever is in the box, so one click sent the page's own subtitle as the reader's question — and the server appended the same string to the seed as its parenthetical, so it arrived twice. The clamp is the second half: `desc` is unbounded (mimir's longest is 1816 chars, 186 pages exceed 400) and an unclamped hint pushed the Send button below the popover fold. Send stays disabled while the question is empty (the route 400s it). The thread-name PREVIEW derives from the article title via `chatOptNameSource` + `previewThreadName`, mirroring the route's own two-call fallback exactly — `chatOptNameSource` runs `deriveAskThreadTitleOrNull`, NOT `.trim()`, or a control-character title previews the generic `wiki ask` while the route stores the question-derived name — and typing the question deliberately does NOT repaint that preview or clear a 409 (the collision is on the name, and "Send there →" still needs the thread id).

Four popover-lifecycle rules this mode added, all pure-tested in `wiki-chat-target.ts`:
- **Focus + caret survive the re-render.** The question row renders ABOVE the loading return so the reader can type immediately, and `loadChatTarget`'s `finally` (plus the user/connector pickers) re-renders the panel with `panel.innerHTML = …`, replacing the textarea mid-word. `renderChatOptions` captures `captureChatOptFocus(document.activeElement, panel.contains(active))` before the swap and re-focuses + `setSelectionRange`s after it (guarded — a `<select>` has no range).
- **A `nameTaken` 409 offers no "Send there →" at all.** `chatOptConflictFootHtml({nameTaken})` renders the "Start new thread" button ALONE and as the primary (no `ghost`), and `conflictStatusLine(typed, mode, true)` says the name is held by an unrelated chat. See `src/wiki/CLAUDE.md` for the route-side identity tag that produces it; the client sets `conflict.existingThreadId = ""` precisely because there is no thread to send to.
- **An empty question disables BOTH conflict buttons** and the "Type a question first." guidance now renders UNDER the conflict copy instead of being suppressed by it (`chatOptStatusLines` returns a LIST). Live buttons on an empty question fired a blank tab that opened and closed with no feedback.
- **Escape confirms before discarding, and navigation closes the panel.** An article question exists only inside the panel (direct mode's draft lives on in the Ask box), so `chatOptEscapeAction` returns `confirm` for the first Escape on a non-empty one (status line says "press Esc again"; typing disarms it) and `close` for the second. `renderBreadcrumb` calls `shouldCloseArticleChatOnNavigate` BEFORE re-stamping `currentArticle`, since a popover left open across a navigation targets the page the reader just left and its anchor is already detached; `hideBreadcrumb` clears `currentArticle` for the same stale-state reason. `desc` reaches the client through the additive **`includeDesc` option on `toListing`, passed by the SINGLE-PAGE `/api/wiki/page` `meta:` call ONLY** — not its `listings()` helper (the outgoing/backlink arrays, which are link-heavy pages' bulk) and never the hot `/api/wiki/pages` listing (~100 KB of it on jarvis); all three callers share that one function, so the strip comment there names each one and any change must check all three (regression-tested per caller). The page the popover acts on is `currentArticle`, stamped by `renderBreadcrumb` (and re-stamped from the single-page payload on the explainer path) rather than looked up in `allPages` — the listing is exactly the payload that strips `desc`. `/api/wiki/digest` is the start view's **What's new** card: `generateWikiDigest` (`src/wiki/digest.ts`) reads the wiki's `log.md`, summarizes the recent window (≤14 days / ≤30 entries / ≤15 KB) via one `executeOneShot` call on the wiki's synthesis bot (owner-routed via `resolveWikiSynthesisBot`, same as Ask), and marks resolvable page mentions as `[[wikilinks]]`. Cached per wiki in an in-memory Map keyed by name (mtime-validated via `digestCacheDecision`; `?refresh=1` bypasses); a wiki with no `log.md` returns `{ digest: null }`. Stored bullets stay plain markdown — the route renders them to reader HTML at response time. The `/wiki` page also annotates each picker option with the wiki's freshness date (`log.md` mtime date, fallback newest page date). `GET /api/wiki/index-coverage?wiki=` is the start view's read-only **Index** card: it compares the wiki's on-disk `.md` pages against the deduped UNION of its backing `collections`' huginn doc ids (huginn doc `id` IS the wiki-relative path) and reports `missing` (actionable `.md` gaps — pages a collection SHOULD index but doesn't), `excludedByRule` (`.md` pages no collection would ever index by design — the meta denylist `index.md`/`log.md`/`CLAUDE.md`/`plans/*`/dotfiles, or out-of-scope of a scoped `^life/.*` include; informational, NOT missing), `ghosts` (indexed ids with no file — restricted to `.md`/`.html` ids so mimir's indexed `.gitignore`/`.txt`/`.json` don't show as false ghosts), `htmlPages` (explainers — informational, never counted missing), plus `indexed`/`totalMd`. **Pattern sourcing:** the exclude/include rules come from huginn's `GET /api/collections` (`includePatterns`/`excludePatterns` per collection — one degrade-tolerant call, **no hardcoded pattern copies in muninn**); the pure layer mirrors huginn's `FilesDocumentReader` semantics — BOTH pattern sets applied with Python `re.fullmatch` (anchored `^(?:pat)$`), matched against the posix wiki-relative path in ORIGINAL case (huginn regexes are case-sensitive). A page is `excludedByRule` only when EVERY collection would never index it AND it isn't in the indexed union; an indexed page stays `indexed` whatever the rules say. **Graceful degrade:** an older huginn (no pattern fields) or an unreachable `/api/collections` ⇒ no partition, meta pages fall back into `missing` (the pre-#243 behavior). Membership compares on a normalized key (`normalizeRelPath` + Unicode **NFC** — the NFC fold stops macOS-NFD vs huginn-NFC Norwegian names showing as false missing+ghost pairs). Counting is **per-file** (not per-unique-key) so a case-only-distinct `Foo.md`+`foo.md` pair both count and the invariant `indexed + missing.length + excludedByRule.length === totalMd` holds. Pure logic + the degrade contract live in `src/wiki/index-coverage.ts` (`computeIndexCoverage`, `buildIndexCoverageResponse`); the route sources listings via the shared `listCollections` helper (`src/summaries/list-collections.ts`, also used by the summaries stats/backlog paths). Never-5xx: a wiki with no `collections` (or unknown / dir-missing) returns 200 + `{ error }` with null coverage fields; a failed collection listing returns 200 + `errors[]` AND **suppresses** the coverage fields (`totalMd`/`indexed`/`missing`/`excludedByRule`/`ghosts` → null — a partial union would flag really-indexed pages as missing), `htmlPages` stays. `?refresh=1` busts the page-index TTL cache only (listings aren't muninn-side cached in v1). The card (client `wiki-browser.ts`, styles in `wiki-page.ts`) loads lazily on the start view next to the What's-new card (fetch-coalesced like the digest — a manual refresh racing the lazy load can't double-fetch); missing/meta pages link into the reader, missing/meta/ghost lists expand on click; a degraded/failed fetch shows a quiet "index status unavailable" line that still surfaces the preserved informational counts (htmlPages), a no-collections wiki hides the card. **Uncommitted-changes badge:** the response also carries `dirtyCount` + `oldestDirtyMtimeMs` (`wikiDirtyStat` in `src/wiki/commit.ts` — a cheap `git status --porcelain` line count over the wiki subtree + the oldest dirty file's mtime; 0/null when not a git repo, never throws, attached by the route independent of collections). The card renders an "uncommitted changes: N" badge only when N > 0 — amber, flipping **red** once the oldest dirty file is > 24h old (the daily `wiki-committer` sweeper should have caught it). **Manual reindex (PR):** the Index card carries a **[Reindex now]** button backed by `POST /api/wiki/reindex?wiki=` (fans huginn's `POST /api/collections/<c>/update` over each backing collection) + `GET /api/wiki/reindex-status?wiki=` (proxies each `/api/collections/<c>/update-status`). Same registry resolution + `?bot=` alias + clean no-collections error as index-coverage. huginn's CAS **409** (a rebuild — nightly job or a prior trigger — already in flight) maps to the honest `already-running` state, **not** a failure; an unreachable huginn ⇒ 200 with per-collection `error` entries; a failed status fetch ⇒ `status: "unknown"` + error for that entry. **Never 5xx** and **no muninn-side mutex** — huginn's CAS is the single serialization point. The client renders per-collection state inline, then polls status every 3 s until no collection is `running` (gives up quietly after 3 consecutive poll failures, survives the card disappearing mid-poll), disables the button for the cycle, and on settle re-fetches `index-coverage?refresh=1` so missing/ghosts reflect the fresh index; a terminal `failed` status renders huginn's error text on the card (first visibility into a silently failing nightly job). Response-assembly (409→`already-running` mapping, unreachable→error entries, multi-collection fan-out, status→`unknown` degrade) is the pure/injectable `src/wiki/reindex.ts` (`buildReindexResponse`/`buildReindexStatusResponse`); the route implements the HTTP posters via `fetchKnowledgeApi` (reading `KnowledgeApiError.upstreamStatus` to detect the 409). **Listing freshness — load + focus/heartbeat refetch (freeze while reading):** `GET /api/wiki/pages` is the reader's ONLY page set, and it carries `Cache-Control: no-store` (#413) so a refetch always sees the real index. The client no longer fetches it once: `wiki-browser.ts` re-pulls it on `window` **focus**, on `visibilitychange`→visible, and on a `WIKI_REFETCH_TICK_MS` (5 min) `setInterval` heartbeat — the last because a tab left VISIBLE on a second monitor fires neither event, i.e. the canonical hours-stale tab is exactly the one the event-driven path can never reach. All three run through one throttle, `WIKI_REFETCH_MIN_INTERVAL_MS` (30 s, stamped at REQUEST time — including the boot load — so a hung fetch can't let a pile-up through). **The focus/visibility paths send `?refresh=1`; the boot load and the heartbeat do not.** That split is load-bearing, not a tuning choice: the store's index cache is a 5-minute TTL (`CACHE_TTL_MS`), so a PLAIN refetch usually returns the very listing the tab already has and the feature would deliver nothing for up to five minutes — precisely the staleness it exists to fix. A user actively returning to the tab is worth the ~140 ms warm rescan (measured on the 953-page wiki, bounded by the 30 s throttle); idle drift is well served by TTL freshness. **Ordering:** every request carries a sequence number (`startFetch`) and a response older than one already folded in is dropped, so a slow boot fetch can't revert a newer refetch — and the boot response goes through the SAME `receivePages`/apply discipline, keeping only its extra work (the coverage footer + the one-time navigation from `location.search`) on the first successful adopt. **A failed BOOT is healed, not half-healed:** `bootRendered` flips only when something actually rendered, so the first successful refetch after a boot that painted the error pane runs the full boot render path instead of populating the list beside a permanent error; boot's own error paints are suppressed once anything has rendered, and the payload is shape-guarded (`Array.isArray(data.pages)`) rather than only `data.error`-checked. A failed/degraded refetch is otherwise SILENT (the last good listing stays). Two adoption guards live in the model: an `error` payload with zero pages is always discarded, and a well-formed EMPTY set never replaces a non-empty listing (a transient mid-checkout scan returns no `error`) — while a genuinely empty wiki IS adopted at boot. A third is **skip-if-unchanged**: `pagesFingerprint` neutralises `scannedAt` in the RAW response text and a byte-identical payload is dropped before any render (measured: 9 of 10 refetches were identical 481 KB payloads re-rendering 953 rows for nothing). **A fresh set is applied only on the browse/start view**: applying it under an open article — or under a painted Ask/Explain/fact-check answer, or DURING a navigation whose response hasn't landed yet (`viewStateOf(currentName, navInFlight, hasAnswerBody)` — `currentName` is only assigned from the page response, so without `navInFlight` the whole round-trip reads as "start" and re-sorts under the row just clicked) — would re-sort the left list under someone reading, so it is stashed and adopted at the next `renderStart`/`loadPage`/`loadPageByRelPath`, exactly once (`takePending`). The deferred apply is **data + facets only** and lets the caller's own render paint, so a user action costs ONE full list render, not two. The apply repaints only what the payload derives — folder/type/status/tag facets, the list, the start-view type counters (`#wikiStartStats`) and the start body, plus the coverage footer — so `filters`, the search box and the sort select (all held outside that path) survive; `syncFilters(autoOpen=false)` on this path so a background adopt can never re-open a deliberately collapsed Filters stack (only user actions may); `renderList` itself captures/restores `#wikiList`'s `scrollTop` (the `renderBacklog` precedent — owning it inside the function that replaces the rows is what makes the guarantee hold on the deferred paths, where the caller's own render would otherwise undo a restore done at the refresh call site); and a selection whose page vanished simply stops matching a row (the existing not-found non-event). `refreshStartBody`'s Atlas early-return is likewise load-bearing here: a background adopt must not wipe an in-progress Atlas exploration, at the accepted cost of the projection staying frozen until the next tab switch. **Facet controls never delete their own active value:** `facetKeys(counts, active)` (`wiki-filter.ts`) unions the active folder/type/status into the option list even at count 0 — the folder picker previously "solved" a zeroed selection by CLEARING `filters.folder` from inside a render, i.e. a background event silently rewriting a filter the user set. Digest / Similar / Ask / index-coverage are deliberately NOT refetched. The throttle, ordering, guards and the apply/defer decision are the DOM-free `views/components/wiki-refresh.ts` (`shouldRefetch`/`startFetch`/`viewStateOf`/`receivePages`/`takePending`/`markApplied`/`pagesFingerprint`, unit-tested — the `wiki-explain.ts` split rationale; the helpers MUTATE the model they are handed, so "DOM-free", not "pure"). Smoked in `e2e/wiki-refresh.spec.ts`, which boots its own muninn on port 3024 with a temp `WIKI_EXTRA` wiki, writes a page AFTER load, asserts the plain route still can't see it, drives the focus listener under Playwright's fake clock, and counts `#wikiList` childList mutations to hold the one-render-per-action rule). |
| `wiki-gardener-routes.ts` | `/wiki/gardener` review gate + `/api/wiki/proposals` list & `:id/{approve,reject}` — approve runs the apply step (writes the page into the wiki, updates log.md, reindexes). Also `GET /api/wiki/linter-findings` — report-only wiki hygiene findings (`src/wiki/lint.ts`), recomputed on demand (no DB table); backs the **Lint findings** section on `/wiki/gardener`. **Not bot-gated** (unlike every other route in this row): linting is a pure index read, so it serves any registered wiki including the standalone `WIKI_EXTRA` ones, which previously got the refusal `"the linter is only available for bot wikis"` and therefore had no lint anywhere; the client loads it unconditionally, even on a wiki this page reports as gardener-unavailable. The proactive `wiki-linter` WATCHER stays bot-only by design (it needs a bot's Telegram delivery channel). Also `GET /api/wiki/ingest-backlog?bot=` — report-only "queued up" counter: per summary collection, how many all-time summary docs have never been ingested into the wiki (URL-referenced-wins / consumed-wins credit rules; pure partition in `src/wiki/ingest-backlog.ts`). Fetches the `SUMMARY_SOURCES` collections sequentially + sweeps every wiki `.md` for URLs; per-bot in-process cache (5-min TTL, single-flight, `?refresh=1`), degrades to 200 + `errors[]`. Backs the backlog strip on `/wiki/gardener` and the "all-time backlog" line in the `/summaries` Stats tab. Same bot resolution + never-5xx contract as the linter route. Missing/unreadable wiki ⇒ 200 with an `error` field. **Backlog drain (PR 2):** the extended GET adds live fields (`running`/`offered`/`remaining`/`lastBacklogRun`/`watcherSeeded`/`progress` + the batch constants `batchSize`/`maxProposals`, echoed from `src/gardener/backlog.ts` so the client's confirm panel never hardcodes them) merged OUTSIDE the cache (`mergeBacklogLiveFields` — never mutates the cached object; strips the server-only `queuedKeys`). `progress` is the live drain projection (`getBacklogProgress`: `{stage, draftsDone, draftsTotal, currentTopic?, startedAt, cancelRequested}`) — null when idle OR when a weekly run holds the mutex (that path passes no `onProgress`, so its `Running…` state is deliberately non-cancellable). `POST /api/wiki/gardener/backlog-run` kicks a DETACHED gardener run over a bounded batch of never-ingested docs through the SAME pipeline (`startBacklogRun`/`assembleBacklog` in `src/gardener/backlog.ts` + `buildGardenerSeams` from `src/watchers/wiki-gardener.ts`); responds `{state: started\|running\|…}` immediately, no Telegram alert. The run's work fn owns the progress-map writes + the `cancelRequested` read and threads them into `runGardener` as hooks (`onProgress`/`shouldAbort`/`onAborted`); the route's closure only forwards them. `POST /api/wiki/gardener/backlog-cancel` soft-cancels the in-flight drain (`requestBacklogCancel` flips the run's cancel flag; the runner stops after ≤1 more draft, keeps every persisted proposal, returns the undrafted clusters' docs to the offered set); no run in flight ⇒ no-op 200 `{cancelled:false}` (same resolution/watcher-seeded guards as reset). `POST /api/wiki/gardener/backlog-reset` clears the offered-memory snapshot. **Crash safety (PR 3):** the drain persists a **run journal** to `watcher_snapshots` key `backlog:run` (`{startedAt, batchKeys}`) BEFORE offering the batch, and the last-run outcome to `backlog:lastRun` (durable fallback the GET reads after a restart drops the in-memory map). The journal is cleared only on a success/cancel settle — **KEPT on the error settle** (a `runGardener` throw strands its batch like a crash). The extended GET adds an `interrupted: {at, batchSize, drafted}` live field when a journal exists AND no run is in flight (`drafted` = journal batch keys appearing in the bot's proposals' `source_docs` with `created_at ≥ startedAt`, the shared pure `draftedKeysSince` scan — the time bound stops an older rejected proposal from being miscounted). `POST /api/wiki/gardener/backlog-recover` returns the undrafted batch docs (`batchKeys − draftedKeys`) to the offered pool and clears the journal, under the per-bot mutex (`runExclusive` — a recover racing an Ingest serializes, not interleaves; run-in-flight ⇒ 409; no journal ⇒ no-op 200 `{recovered:0}`); `POST /api/wiki/gardener/backlog-dismiss` clears the journal only (leave the batch skipped) — also under the mutex with the same 409, so a stale banner's Dismiss can't null a live run's journal. A fresh Ingest **auto-recovers a pending journal in-mutex as its first work-fn step** (never a route pre-flight — that's a TOCTOU) so a click before Recover/Dismiss can't orphan the strand. The `BacklogRouteDeps` offered read/write is generalized to per-key `getSnapshot`/`setSnapshot` (offered / run / lastRun share the table) plus `listProposals`. **Age floor + insufficient guard (#288/#289):** the drain only selects docs at least the bot's resolved `lookbackDays` old (shared `passesAgeFloor` predicate; undated docs stay eligible; fresh docs belong to the weekly watcher — this stops zero-draft runs from permanently burning fresh arrivals into the offered set). The GET's `remaining` applies the same floor (server-only `queuedKeys` carry `{key, id, collection, date?}`) and emits `offeredStillQueued` explicitly — the strip must NOT derive it as `queued − remaining` (the floor broke that identity). **Recency-first strip (#302):** the GET also emits `fresh`/`freshBySource`/`freshWindowDays` — un-offered docs still INSIDE the age-floor window, per source (the weekly watcher's turf; `fresh + remaining + offeredStillQueued === queued` exactly). The strip leads with "N new (last 14d): YouTube n · …", then "N drainable now"; the all-time accounting collapsed into a `<details>` tail (open state survives the 3s drain-poll re-renders). **Coverage framing (R2, backlog-legibility):** the tail summary is an honest coverage sentence — "N of M all-time docs have no wiki footprint (pre-auto-drafter tail; …)" (queued of `data.total`, denominator floored at the queued count on a degraded response), collapsing the redundant "offered-in-past-runs / never-ingested" pair; the toggle body is a per-source "queued of total" breakdown. Needs per-collection `total` + grand `total` on the model (`allTimeTotal`, `perSource[].total`). `freshWindowDays` doubles as the degraded-response marker (0/absent ⇒ fresh segment hidden, never a false "0 new" — safe because `resolveGardenerConfig` rejects `lookbackDays ≤ 0`); the model field is `nothingDrainable` (née `allOffered` — the trigger can be all-fresh, not all-offered). A batch below `minClusterSize` (now a `StartBacklogRunDeps` field, resolved at the route call site) writes nothing — no journal, no offered persist, no `runGardener` — and records `outcome: "insufficient"` (+ `eligible`, `minClusterSize`) on `lastBacklogRun`; `backlogOutcomeHtml` renders it. **Zero-draft rollback (#311):** a completed un-cancelled run that persists zero proposals rolls the offered snapshot back to its pre-batch state (`startBacklogRun`'s zero-draft branch) and reports `offered: 0`. **Zero-draft reason line (R1, backlog-legibility):** the old `offered > 0 && drafted === 0` `.bk-warn` burn branch is now UNREACHABLE (the #311 rollback means a completed zero-draft run always reports `offered: 0`; a *cancelled* one hits the `cancelled` branch, an errored one the `error` branch) — so it's folded into an honest reason line. `runGardener` gained an optional `onTally(dropTally)` hook (mirrors `onAborted`) firing once after clustering with the AGGREGATE drop tally (the same `dropTally` over `allDropped` the per-run log prints, NOT the span-scoped `clusterTally`/`gateTally` partials; not fired on the harvest-floor early return). The drain's work fn captures it and persists `dropTally` + `attemptedDocs` (= `assembled.batchKeys.length`) + `minClusterSize` on `LastBacklogRun` (declared twice — journal shape in `backlog.ts`, client render shape in `wiki-gardener-strip.ts` — mirrored). `backlogOutcomeHtml` then renders "last run drained N docs, none clustered — all below cluster size (need M on one topic)" (or surfaces skip/duplicate/cap/hallucinated buckets when present; falls back to the attempted-doc count when no tally, e.g. the harvest floor). Doc counts and cluster-drop buckets are kept distinct in the copy (tally counts CLUSTERS). **Low-volume source fallback (R4):** the THREE zero-draft paths — the insufficient short-circuit, the harvest floor, and the cluster-size gate zeroing a batch that ran — fall back to drafting the batch docs individually as SOURCE pages (`runSourceFallback` in `src/gardener/backlog.ts`, injected as the `draftSourceFallback` seam on `StartBacklogRunDeps`, bound at the route to the now-exported `draftOneBacklogDoc` via `defaultSourceBacklogDeps`) instead of returning empty, so X/Anthropic/article verticals always produce something reviewable. **Draft-failure exclusion:** the completed-path fallback is gated on `!(keptClusters > 0)` — it must NOT fire when clusters DID form and pass the gate but every draft attempt failed transiently (timeout / shape-gate). Those docs are cluster-worthy; converting them to per-doc source pages would make them pending and never re-clustered (permanent loss of concept synthesis on a transient failure) and would render the "(fallback — nothing clustered)" lie the R1 draft-failure branch exists to prevent. `keptClusters` is undefined on the harvest-floor early return (no tally) — the fallback SHOULD still fire there, hence `!(keptClusters > 0)` not `=== 0`. It also honors a soft cancel mid-fan-out (`shouldStop` bound to the run's `cancelRequested`) and drives a "drafting" progress stage so the UI isn't wedged while up to 8 one-shots run. The fallback runs inside `startBacklogRun`'s mutex-held work fn, fans out over `assembled.batch` (the newly-returned `BacklogCandidate[]` — collection/id SEPARATE, so no `<collection>/<id>` key parsing that slashed doc ids would corrupt), fetches each body internally (the drain discards harvested bodies), caps at `BACKLOG_MAX_PROPOSALS` (8) REAL model attempts (cheap covered/skipped don't consume the cap; one doc's error never aborts the rest), and persists a DISTINCT `fallbackDrafted` count on `LastBacklogRun` (NOT `drafted`, which counts gardener CLUSTER proposals — so the #311 rollback, keyed on `drafted === 0`, still fires and the fallback's drafted docs get credited as pending via their own proposals). `backlogOutcomeHtml`/`backlogZeroDraftReasonHtml` render "drafted N source pages (fallback — nothing clustered)" (or the insufficient-path "drafted N source pages individually (fallback)") when `fallbackDrafted > 0`, winning over the "none clustered" reason. `fallbackDrafted` is mirrored in BOTH `LastBacklogRun` shapes (journal in `backlog.ts`, client render in `wiki-gardener-strip.ts`). **Run-gardener-now (#308):** the live fields also carry a `watcher` block (`{id, enabled, lastRunAt, nextRunAt, forceQueued}` from `getWikiGardenerWatcher`; `nextRunAt` via the canonical `computeWatcherNextRun` projection — hour-gate-aware, mapped to null when ≤ now) so the strip's fresh segment shows "next weekly run in ~Nd" + a **Run gardener now** button (gated `fresh > 0 && enabled && !running && !forceQueued`) that POSTs the existing `/api/watchers/:id/trigger`; a queued note (suppressing the next-run text) covers `force_next_run` until the scheduler tick starts the run, and `pollBacklogUntilRunStarts` hands off to the drain poller on `running`. Watcher path writes no offered-memory; disabled/absent watcher degrades to the plain "weekly watcher's turf" note. **Source-page drafter (#310 run-now + backlog button):** the per-article `.mdx` source drafter (`src/gardener/source-drafter.ts` — one encyclopedic source page per capture, into the SAME `wiki_proposals` gate under kind `source`) has two on-demand dashboard entry points. `POST /api/wiki/gardener/source-draft-run?collection=` drafts the NEWEST doc in a collection (default `youtube-summaries`) — one traced model one-shot, awaited, 200 outcome / 500 on error (`runSourceDraftForNewest`). `POST /api/wiki/gardener/source-draft-backlog?collection=&limit=N` drafts up to `limit` UNCOVERED docs (`runSourceDraftBacklog`) — batch selection reuses the ingest-backlog partition verbatim (`computeIngestBacklog` over `collectWikiRefs` + consumed/pending sets), then drafts sequentially (never parallel — each is a real one-shot on the resolved wiki bot, NOT the summarizer bot). **Oldest-first (R2, backlog-legibility):** `selectSourceBacklogDocs` sorts the uncovered queue OLDEST-first (ascending date; undated docs LAST via `+∞`, the inverse of the drain's newest-first `−∞` convention) so the "Backfill oldest" button eats the chronological tail; undated docs are deferred (R5 archive-tail territory). **Head-of-line fairness (#312):** the loop scans the FULL queue in order and counts only real MODEL attempts (`drafted`/`error`) toward `limit`; cheap deterministic skips (`skipped`/`covered` — no body/url, url-covered, stem collision) don't consume the limit and the loop continues deeper, so a poisoned head can't wedge the batch on the same unreachable tail every click (`totals.selected` is the VISITED count, so it can exceed `limit`). `limit` clamps to `[1, SOURCE_BACKLOG_MAX_LIMIT=10]` (default 3); skip-not-fail per doc (a fetch/draft failure is a recorded `error` outcome, never aborts the batch); response is per-doc `{outcome, reason?, proposalId?, title?}` + rolled-up `totals` + `totalQueued`. Nothing auto-approves — drafts land in the gate below. Both gate on `gardener.enabled !== false` and use the shared `resolveBacklogBot`; the backlog route also serializes under the per-bot gardener mutex (`runExclusive` — try-lock, so a click racing a drain or another click gets a **409** rather than double-spending model calls). The strip's **Backfill oldest** button (`backlogSourceDraftHtml`, gated `sourceDraftAvailable = !running && gardenerEnabled !== false && youtube-summaries queued > 0` — youtube-specific because that's the only collection the action drafts; hidden when the gardener is disabled since the route 400s then; label is limit-free because the client sends no `limit` — the "3" is a route-side default absent from the GET response) POSTs the backlog endpoint on an explicit click; the client disables it to "Drafting…" while the batch awaits and renders the returned totals via `sourceDraftResultHtml`, then refreshes the strip + proposal list so fresh drafts appear. Every seam of `runSourceDraftBacklog` is injectable (`SourceBacklogDeps`) so the selection/limit/skip logic is unit-tested (`source-drafter-run.test.ts`) with no real model calls. **Weekly-run drop-tally parity (PR 2, watcher-path):** the WEEKLY `checkWikiGardener` now wires the same `onTally` hook the drain uses and persists a DISTINCT `WeeklyGardenerRun` (`{finishedAt, clustersFound, kept, dropped, dropTally, evictedTopics}`) to `watcher_snapshots` key **`gardener:lastRun`** — deliberately separate from the drain's `backlog:lastRun`/`LastBacklogRun` (whose `attemptedDocs`/`fallbackDrafted` shape would collide). `onTally` gained a third arg (the raw `ClusterDropEntry[]`) so the snapshot stores the LOSSLESS structured `evictedTopics` tail (`{topicKey, reason, size}`) instead of the tally's trace-capped 500-char `clusters_dropped_topics` string; the drain's hook ignores it (fewer-params assignability, byte-identical). The extended GET reads the snapshot into a new `weeklyRun` live field (snapshot-only — the watcher fires out-of-band, no in-memory record); the strip's own `weeklyRunHtml` branch (NOT the drain's `backlogOutcomeHtml`) renders "last weekly run: 26 clusters found, 3 kept (cap) — 23 dropped" (the `(cap)` naming the dominant drop reason), with the full evicted-topic list in a lossless `title` tooltip. `WeeklyGardenerRun` is declared twice (write-side in `backlog.ts`, client-render mirror in `wiki-gardener-strip.ts`), same client-bundle-hygiene pattern as `LastBacklogRun`. **Backlog inspector (PR 1):** `GET /api/wiki/ingest-backlog?docs=1` additionally emits `docs` — every queued doc decorated with its live `bucket` (`fresh`/`drainable`/`offered`), `date`, `label` and `url`. Classification happens at the LIVE-merge layer, not inside the 5-min-cached compute (`computeBacklogFloorCounts` now returns the classified `docs` alongside its three counts, from the SAME single pass — a count and its bucket membership can never disagree), so buckets reflect the per-request offered set; the cached `queuedKeys` gained `url` for the row's "open ↗". The count-only default payload is otherwise unchanged apart from one **additive** field — `freshBySource` rows now carry `collection` on every response (the per-source toggle's scope key) — and the `docs` list itself is strictly opt-in, so the strip's 3s drain poll stays count-only; doc-list construction is likewise gated on the `wantDocs` flag (`computeBacklogFloorCounts(..., withDocs)`) so the poll never allocates or labels a row per queued doc. Row `label` is the doc id's **basename, extension stripped** (`backlogDocLabel`) — huginn's listing carries no populated `title` and ids ARE readable source paths; deliberately no N per-doc title fetches. Both the rendered date and the newest-first ordering key off `docDateMs` (`backlogDocDate` + `sortBacklogDocsNewestFirst`) — the SAME date the drain selects by, so an undated listing row whose id carries a `YYYY-MM-DD` prefix shows that date and sorts by it instead of rendering "—" at the bottom; only a genuinely dateless doc sorts last. Client: every count in the strip (fresh total, per-source fresh rows, "N drainable now", the tail's per-source rows, and an offered chip in the tail — **"Reset offered (N)" stays a pure action button**, never overloaded) is an `inspectToggle` opening ONE shared filterable panel (`backlogInspectorHtml` + `filterBacklogDocs` + `backlogDocHref`, pure/unit-tested in `wiki-gardener-strip.ts`). The panel's open/bucket/collection/paging state lives at **module level** in `wiki-gardener-browser.ts` (the `backlogConfirmOpen` pattern) because `renderBacklog` replaces the strip's innerHTML wholesale every poll; rows lazy-fetch on open and render `INSPECTOR_PAGE_SIZE` (200) at a time. `renderBacklog` also captures/re-applies `.bk-inspector-rows`' `scrollTop` (same guard as the tail `<details>` open state — a poll tick must not scroll a reading user back to the top), and every MUTATING path (a drain settling, Reset offered, Backfill oldest, recover) calls `refreshInspectorAfterMutation()` — ONE refetch per event, never per tick — so an open panel can't keep showing drained rows still chipped `drainable`. A collection with 0 queued docs renders its tail count as plain text (no toggle) and is dropped from the panel's collection `<select>` (the currently-selected one is always kept as an option); a failed refresh renders its error as a note ABOVE the still-visible rows, and opening the panel clears any stale `error` first. Deep links are `/search/document/<collection>/<encodeURIComponent(id)>` — the id segment is ALWAYS encoded (real ids carry `/`, spaces, `#`, non-ASCII; naive interpolation truncates at `#`). **Prune verbs (PR 2):** the inspector gained **dismiss** (cheap, reversible) and **delete** (destructive, huginn-side). *Dismiss* is a `backlog:dismissed` key set in `watcher_snapshots`, a sibling of `backlog:offered`, applied at TWO distinct touchpoints. **Counters:** a FOURTH bucket in `computeBacklogFloorCounts` (new 6th arg `dismissedSet`), emitted as its own `dismissed` wire field, keeping `remaining + offeredStillQueued + fresh + dismissed === queued` exact. **Precedence: dismissed WINS over offered** (checked first) — dismissed means "never select, drop from every actionable counter", so if it counted as offered instead, `Reset offered` would silently un-dismiss it; the intended side effect is that dismissing an offered doc SHRINKS `offeredStillQueued` and thus the Reset button's count/visibility. The set is read per-request beside the offered set, deliberately **outside** the 5-min cache and **never** inside `computeIngestBacklog` (TTL-cached, three other callers, tested `total === ingested + queued` invariant) — so a dismiss lands on a PLAIN refetch and the client must never send `refresh=1` for it. **Selection seams** (counters alone don't stop model spend) — the set is unioned into all three: `assembleBacklog`'s batch exclusion (new optional `getDismissed` dep; deliberately NOT folded into the returned `offeredBefore`, which the caller persists back to `backlog:offered`), `selectSourceBacklogDocs`/`runSourceDraftBacklog` (new optional 5th arg / `SourceBacklogDeps.getDismissed`; `totalQueued` reports the ACTIONABLE queue), and the weekly harvest via `weeklyConsumedWithDismissed` (`src/watchers/wiki-gardener.ts`, exported + injectable) folded into `consumedDocIds` — the weekly path's only exclusion seam, so without it a doc dismissed today is clustered by the very next weekly run. **Presentation split (deliberate, stated):** the `/wiki/gardener` COVERAGE sentence keeps counting dismissed docs (it is a statement about wiki FOOTPRINT — dismissing doesn't ingest anything) and names the share in its note (`…; N dismissed)`); the `/summaries` Stats **all-time backlog** line reads as ACTIONABLE and therefore SUBTRACTS them, rendering `N (+M dismissed)`. Routes: `POST …/backlog-docs-dismiss` · `…/backlog-docs-undismiss` · `…/backlog-docs-dismiss-reset` (`{keys: string[]}`, capped at `MAX_DISMISS_KEYS`=2000) — note the `-docs-` infix, since the pre-existing `backlog-dismiss` clears the interrupted-run JOURNAL. Guards are the established precedents: `watcherSeeded` 404 exactly as `backlog-reset` (the snapshot is keyed by watcher_id, so the affordance is hidden client-side via the model's `pruneEnabled`/`showDismissReset`) and mutex 409 via `runExclusive` exactly as `backlog-recover` (reset itself does NOT take the mutex — it delegates its in-flight check to `resetBacklogOffered`). *Delete* is `POST …/backlog-doc-delete` `{collection, id}`, a **server-side proxy** of huginn's `DELETE /api/document/{collection}/{doc_id}` (huginn's CORS is GET/POST-only, so the browser cannot call it). Single-row only in v1, with a per-doc confirm naming the doc. **Refresh timing is the crux:** huginn's 200 means only "source moved outside `reader.basePath` + background reindex enqueued" — the doc leaves its LISTING later, so an immediate `refresh=1` would re-list it and cache the wrong payload for the full TTL. The route splits huginn's `reindex` map into `polling` (`started`) vs `skipped` (`skipped_already_running` — that run began BEFORE the move, so its success says nothing about this delete; surfaced honestly as "doc moved; may still be listed until the next index run" rather than polled), the client renders the row as "removing…", polls `GET …/backlog-doc-delete-status?collection=` (a proxy of `/api/collections/<c>/update-status`) at 2 s until terminal or a 120 s stop, and ONLY THEN re-fetches with `refresh=1`. That status route is also the **cache-invalidation seam**: on `succeeded`/`failed` it calls `invalidateIngestBacklogCache(bot)` (drops the entry AND any in-flight compute — `refresh=1` bypasses the cache READ but would otherwise join a stale in-flight promise). An abandoned poll heals at TTL expiry. On success the key is pruned from BOTH the offered and dismissed snapshot sets. UI: a `dismissed` bucket filter + per-row dismiss/un-dismiss/delete, a **bulk dismiss over the whole FILTERED set** (dismiss-only; already-dismissed rows excluded), a `Reset dismissed (N)` chip in the tail beside `Reset offered`, and `BacklogInspectorState.removing` (module-level, so a 3 s poll re-render can't lose it). NB PR 1 shipped `rerenderStrip()` as an infinite self-call (stack overflow on the first inspector toggle) — fixed here to re-enter `renderBacklog(lastBacklogData)`. **Review hardening (PR 2 follow-up):** (a) prune outcomes ride a SEPARATE sticky `BacklogInspectorState.notice` (`{text, kind:"err"|"info"}`), NOT `inspector.error` — every verb ends in a refetch and `loadInspectorDocs` nulls `error` on success, which wiped the 409 and the honest skipped-reindex note within a frame; the notice renders above the rows and is cleared only by the next user action (prune click / filter change / panel toggle). (b) The cache carries a per-bot **invalidation generation**: `invalidateIngestBacklogCache` bumps it, and a compute captures it at start, skipping BOTH its `backlogCache.set` and its in-flight eviction if it changed — otherwise a compute that had already read the pre-delete listing re-poisoned the cache for the full TTL and its `.finally` deleted the newer in-flight promise. (c) `refresh=1` after a delete is CONDITIONAL on a terminal poll: when every collection came back `skipped_already_running` (or no status ever resolved) nothing dropped the server cache, so the client does a PLAIN refetch + a sticky notice instead of re-caching the still-listed doc. (d) The client polls until `succeeded`/`failed` EXPLICITLY (huginn's vocabulary is `idle|running|succeeded|failed`, and the server invalidates on exactly those two); `idle`/unknown/400 gets a small retry budget then counts as unresolved. There is no "first poll sees the previous run's terminal status" race — huginn marks the collection `running` synchronously before its DELETE responds. (e) The live fields carry `dismissedByCollection` (always present) so the ACTIONABLE affordances — the "Backfill oldest" `<select>` counts + `sourceDraftAvailable` + `nothingDrainable` — subtract dismissed docs from the cached per-collection `queued`; the coverage tail deliberately does NOT (footprint semantics). (f) Prune affordances require `watcherSeeded === true` explicitly (an ABSENT field on a degraded server used to leave them live against routes that 404). (g) `runSourceDraftForNewest` gained the same optional `getDismissed` exclusion as the other three seams (the "Draft newest" button no longer spends a model call on a dismissed doc). (h) An over-cap `keys` list 400s with its own message naming `MAX_DISMISS_KEYS`, and the client chunks bulk dismisses into ≤2000-key sequential requests so it can't practically fire. **`scripts/retire-backlog-tail.ts` stays on the OFFERED set by design** (census semantics; `Reset offered` un-retires) — folding it onto the dismissed set is deferred past v1. **Self-explanation (PR 3):** every strip control carries a `title=` tooltip (the segment toggles via the per-bucket `BUCKET_TITLE` map, the meter, Drain/Reset offered/Reset dismissed/Run gardener now, and the row + bulk prune verbs), and `backlogGlossaryHtml` renders a collapsed "how this works" `<details>` at the strip's foot defining new/drainable/offered/dismissed/consumed (the glossary's first term is "new", matching the strip's "N new (last Nd)" label) — the canonical long-form home, since tooltips are invisible on touch. Its open state is captured + re-applied across the 3 s poll re-render exactly like `.bk-tail`'s (`glossaryWasOpen` in `renderBacklog`). **Drafter attempt on the row + per-doc retry (migration 068):** a `?docs=1` row now also carries `draft` — the source drafter's LAST recorded attempt (`attachDraftAttempts` joining `getSourceDraftAttempts(bot)` on `<collection>/<id>`), since three of the drafter's four outcomes persist no proposal and left the row indistinguishable from a never-attempted doc (see the ledger section in `src/gardener/CLAUDE.md`). The join is on the `docs=1` opt-in ONLY (the 3 s poll must not read the table) and an empty map returns the rows BY REFERENCE — a missing migration renders the pre-ledger payload byte for byte. The row renders the outcome + reason + a `/wiki?wiki=&relPath=` deep link to the page that blocked a title collision (`draftAttemptHtml`), plus **draft** / **rename & draft** verbs backed by `POST /api/wiki/gardener/source-draft-doc` `{collection, id, title?}` — same gardener-enabled + mutex-409 guards as its batch siblings, but deliberately NO dismissed-set exclusion (the click is explicit, and the dismiss button is on the same row) and no `watcherSeeded` gate (it writes no snapshot). `title` is capped at `SOURCE_DRAFT_TITLE_MAX` (120) and sanitized in the drafter, since it is interpolated into the prompt. `BacklogInspectorState.drafting` mirrors `removing` (module-level — the poll replaces the strip's innerHTML), and the row markup gained a `bk-doc-row-wrap` wrapper so the "why" line sits inside the same bordered block as its row. See `src/gardener/apply.ts` + the "Wiki Gardener" and "Wiki Linter" sections of `src/watchers/CLAUDE.md`. |
| `indexing-routes.ts` | `/indexing` page + `GET /api/indexing/overview` — huginn indexing-run ledger read surface (proxies huginn's `GET /api/indexing/jobs`). Assembly is the pure, injectable `src/dashboard/indexing-overview.ts` (`assembleIndexingOverview` + `defaultIndexingDeps(knowledgeApiUrl)`); rows grouped into three classes (scheduled / tracked-unscheduled / never-tracked), scheduled first. Never 5xx — huginn unreachable ⇒ 200 + `errors[]`, client renders an error banner. 15s client poll. **Per-collection depth (PR B):** each row expands (client-side, state survives the poll) into a detail area carrying the derived `detail` model on the row — the pure `src/dashboard/indexing-detail.ts` (`buildPhaseTimeline`/`buildSparkline`/`buildIndexingDetail`, unit-tested). Phase timeline from `lastRun.phases`: `ordered` (every phase has huginn-#92 `startedAt` ⇒ sorted chronologically L→R with `→` connectors), `unordered` (ANY phase missing `startedAt` ⇒ arrival order, no axis), or `none` (backfilled `trigger:"unknown"` runs with no phases ⇒ explicit "no phases recorded", never an empty area). A failed non-fatal phase is flagged (`nonFatalFailure`) so `degraded` is visible. Duration sparkline (SVG, no chart lib) from `history` — variant-distinct (per-variant color + trend polyline + legend with medians; x-feed's ~10s incremental vs ~80s rebuild), null durations drawn as a hollow red baseline marker (NEVER a 0 dot), failed points red-stroked. In-flight `current` shows its phases-so-far + elapsed. |
| `logs-routes.ts` | Log file viewer (JSONL files from LogTape) |
| `tools-routes.ts` | MCP tool debug: connect, list, call, disconnect |
| `summaries-routes.ts` | Unified `/summaries` page + `/api/summaries/documents` (merged, source-tagged archive across every summary source — see `src/summaries/sources.ts`) + `/api/summaries/stats` — the **Stats** tab: per-source new-summary counts per calendar month (last 8 months) + 30-day wiki-gardener coverage (consumed / pending / never-clustered vs `wiki_proposals`). Fetches each collection sequentially, parses dates via the gardener's `docDateMs`, aggregates in the pure `src/summaries/stats.ts`; per-bot in-process cache (5-min TTL, single-flight, `?refresh=1` bypass); degrades to a 200 + `errors[]` when huginn/DB is unreachable. |
| `youtube-routes.ts` | YouTube transcript fetch + summarization API. `/youtube` now 301-redirects to `/summaries?source=youtube`; the page lives in the merged view |
| `x-article-routes.ts` | X/Twitter article summarization API (Chrome extension backend). `/x-articles` now 301-redirects to `/summaries?source=x-article` |
| `anthropic-routes.ts` | Claude Learning Center **Curate** layer (Candidates → Summaries). The candidate inbox (`GET /api/anthropic/candidates`, `POST …/:id/{summarize,dismiss}`) + the summarizer vertical (`/api/anthropic/{stream,jobs,document,similar}`, mirroring youtube-routes against the `anthropic-summaries` collection). Summarizes a watcher-captured candidate by pulling its content from Huginn `anthropic-knowledge`; renders on `/summaries` badged "Claude". Also `GET /api/anthropic/candidates/stats` — read-only gate-outcome calibration (per-kind + per-score-band acceptance + suggested `candidateMinScoreByKind`) backing the `/summaries` **Calibration** tab; never writes watcher config. See `src/anthropic/{state,summarizer}.ts` + `src/watchers/anthropic.ts` (capture + ≥0.9 auto-promote) + `candidateOutcomeStats` in `src/db/summary-candidates.ts`. |
| `models-routes.ts` | `/models` page + `GET /api/models/overview` (JSON) — the effective model / connector / Haiku backend for every AI job after all defaults, next to the models actually used in the last 7 days. Assembly is the pure, DB-injectable `src/dashboard/models-overview.ts` (`assembleModelsOverview` over `resolveBackendWithReason` + `resolveSummarizerBot`/`resolveResearchBot`/`resolveWikiSynthesisBot` + `connectorCapabilities` + the `watchers` table + the shared wiki-registry memo + `haiku_usage`/`traces`). A read-only **Wiki synthesis** group (`wikiSynthesis`) lists one row per registered wiki with its resolved synthesis bot + connector/model + a `pinned`/`owner`/`fallback` origin chip (see `resolveWikiSynthesisBot`: explicit `synthesisBot` pin beats owner-gate + fallback); a pin naming no discovered bot is ignored and rendered as a red "pin '<name>' matches no bot — ignored" note (`ignoredPin`). The wiki registry is an injectable dep (`getWikiRegistry`, fabricated in tests). The hard-coded "What's-new wiki digest" role row no longer names a single bot (it routes per-wiki) — it points at the Wiki synthesis group. Diagnostic — surfaces the #191 silent-fallback class. Assembled fresh per request (no cache); degraded sources land in `errors[]`, never a 5xx. **Editing (PR 5):** `POST /api/models/bot-config` `{bot,field,value}` writes one per-bot config.json field (connector/model/thinkingMaxTokens/haikuBackend) IN PLACE via `writeBotConfigField` (`src/bots/config-edit.ts`) — the git-synced source of truth, so it **applies on restart**; validated with `validateBotConfigField` (discovery-aligned enum lists via shared constants; deliberately stricter than discovery on scalars — rejects empty model and negative/non-integer thinking; bad value → 400); the `bot` must name a discovered bot; `value:null` clears the field. `POST /api/models/role` `{role,value}` sets/clears a HOT DB-backed override for `SUMMARIZER_BOT`/`RESEARCH_BOT`/`HAIKU_BACKEND` (`src/db/role-overrides.ts`) — beats the env var at the next resolution with **no restart** (the sync resolvers read an in-memory snapshot refreshed on every write; primed at startup in `src/index.ts` via `loadRoleOverrides`); empty value clears; bot-roles must name a discovered bot (else 400). Summarizer reassignment onto a connector lacking `supportsExtraDirs` returns a non-blocking TikTok `warning`. Every edit is activity-logged (`source: "models-page"`). An active override renders an `override` origin chip in the overview. **Runtime chips (PR 4):** pipeline-job rows carry additive `matchKind`/`matchBot`/`matchName` hints (set on watcher rows + the gardener-drain row); the page client-fetches `/api/agents/overview` alongside `/api/models/overview` and merges live runtime onto each row via the pure `src/dashboard/models-runtime.ts` (`mergePipelineRuntime`/`computeRowRuntime`, DOM-free + unit-tested, hand-mirrored in the models-page client) → a pulsing `running now` chip, a `next: <time>` chip from `upNext`, and a `last <dur>` chip from `recent`. Refreshed on a 15s interval without re-fetching the models overview; a degraded agents fetch just drops the chips. **Redesign (dashboard-redesign PR 3):** the page is rebuilt on the shared PR 1 primitives (`pageHeader*` with the intro moved into the persisted help panel, `summaryTiles*`/`tileHtml`, `statusChipsScript`'s `originChip` — the local `.chip-*` classes are gone). Layout: 3-up **bot cards** (mismatch → warning-35% border + a `used ≠ configured` callout), a per-card "▸ why this Haiku backend?" expandable rendering the real resolution chain, then **Roles + Wiki synthesis** as side-by-side list cards and the **Pipeline jobs** list card. Two assembly additions on `BotEntry` (additive, unit-tested in `models-overview.ts`): `mismatch`/`mismatchModels` (the used-vs-configured predicate `computeModelMismatch` — the bidirectional-substring rule lifted out of the client so border/callout/tile all consume ONE field) and `chain: BackendChainRow[]` (`buildWhyChain` over the new `resolveBackendChain` in `src/ai/haiku-direct.ts`). `resolveBackendChain(opts)` is a non-short-circuiting enumerator over all 7 precedence levels with the winner marked; `resolveBackendWithReason`/`resolveBackend` now DERIVE from it (`chain.find(c => c.wins)`) so ordering lives in one place — invalid-enum fall-through is preserved because the override/env links reuse the same parse helpers (return null ⇒ level doesn't win). Editing flows (in-card config editor, hot role Apply, TikTok warning, toasts) are unchanged. |
| `route-utils.ts` | Shared helpers: UUID validation, pagination parsing |

## View System

All pages are server-side rendered HTML via template literals. No build step, no framework.

### Component Pattern

Each component in `views/components/` exports up to three functions:

- `*Styles()` — returns a CSS string (injected into `<style>`)
- `*Html()` — returns an HTML string (injected into `<body>`)
- `*Script()` — returns a JavaScript string (injected into `<script>`)

Pages (`views/*.ts`) compose components by calling all three and concatenating into a full HTML document.

### Shared Infrastructure

- `views/shared-styles.ts` — CSS variables (colors, spacing), base reset, header, nav. Imported by all pages.
- `views/components/helpers.ts` — Inline JS utilities: `esc()` (HTML escaping), `extractToolInputLabel()`, formatting helpers.
- `views/components/agent-presence.ts` — the **agent presence chip** (mounted on `/wiki/gardener`, `/summaries`, `/wiki`). A tiny, embeddable "is a matching agent live (or about to be)?" indicator: standalone 15s poll of `GET /api/agents/overview` (NO second SSE — the host pages own their own loops), filtered by `{ kinds?, bot? }`, deep-linking to `/agents`. Three states — `running` (pulsing `.pulse-dot` + "<Kind>: <phase> <done>/<total>" label + optional "~Nm left" ETA from the overview `estimates` map), `preflight` (amber "<Kind> starts in Nm" from a matching `upNext` within `PRESENCE_HORIZON_MS` = 60 min), and `none` (renders NOTHING). The pure model builder `computePresence` is DOM-free + unit-tested; the client JS in `agentPresenceScript` hand-mirrors it (keep in sync, like the `computeCardEta` mirror). Each mount is a one-liner styles+html+script composition.

### Pages

`page.ts` (main dashboard — the **Overview/Home** tab was rebuilt for the dashboard-ux-w2 redesign: a `summaryTiles` stat row (attention-border rule) replacing the emoji strip, a two-column body [LEFT = **Now** (live runs via the `agent_runs` SSE event + up-next from `/api/agents/overview`) · **Attention** (`/api/attention`) · a slimmed 7-day **Usage** chart — SVG bars + one total-token line, theme-aware via CSS classes since SVG `fill=`/`stroke=` attrs never resolve `var()`, with the full 4-series Chart.js breakdown behind a toggle], RIGHT = a **single Activity column** [the former bottom drawer folded in, with inline All/Chat/System/Errors filter chips]. `overview-section.ts` owns the render fns; `connection.ts` owns the SSE + fetches. The retired `activity-feed.ts` drawer is deleted; watchers-panel's per-watcher feed filter (`filterFeedByWatcher`/`clearFeedFilter`) still works against the folded-in `#feed`.), `traces-page.ts`, `search-page.ts`, `search-document-page.ts`, `research-page.ts`, `logs-page.ts`, `mcp-debug-page.ts`, `serena-page.ts`, `summaries-page.ts` (unified YouTube + X-article summaries; composes the `sum-*` components, injects the `SOURCES` registry), `models-page.ts` (models overview — three grouped tables: Bots · Role assignments · Pipeline jobs; client-fetches `/api/models/overview`, bot selector re-scopes only the per-bot Pipeline rows), `agents-page.ts` (unified live-agent dashboard, **rebuilt on the PR 1 redesign primitives** — `pageHeader*` + persisted help panel, `summaryTiles*`/`tileHtml`, `statusChips`' shared `kindChip`/`runStatusChip`; the bot pills moved into `headerRight`. Four summary tiles (Running / Up next / Finished / **Spend · last hr**, the last a CLIENT-SIDE approximation over the finished rows carrying `costUsd` within the last hour — "—" when none do, the common case on subscription connectors). A two-column live zone: **N** Running cards stacked LEFT (full #248–#259 observability preserved — tool mini-log, usage line, truthful connector·model chip, ETA/paced bars, Trace/Prompt/Models/Open links, plus the design footer "→ phase") re-rendering on the `agent_runs` SSE event + rAF-ticking elapsed timers/bar widths [two-tier pattern from `request-progress-ui.ts`]; an **Up next** countdown timeline RIGHT (force-queued watchers show their `label` instead of a countdown; imminent items green). A **Recently finished** feed card — 8 rows + "N more · show all" in-place expander (state survives the poll re-render), **status-driven** dots via `runStatusChip` (failed rows loud red), and a **Cost** column via `fmtCost` — `$0.00` for a real 0, a dash for unknown; see the observability `CLAUDE.md` `costUsd` note. All from `/api/agents/overview` + SSE, API shape untouched. The shared `kindChip` palette (`status-chips.ts` KIND map + `.kind-*` in shared-styles) was extended to cover every AgentKind — the page's old local `.kind-*` duplicates are gone.)

## Real-Time Updates

### SSE Endpoints (sse-routes.ts)

- `/api/events` — activity feed events (message_in, message_out, memory, error, etc.), plus `agent_status` (phase), `request_progress` (single-pane waterfall), and `agent_runs` (full AgentRun snapshot for `/agents`, throttled ~1/s)

### Agent Status (`src/observability/agent-status.ts`)

Tracks in-flight requests in a `Map<requestId, RequestProgress>` (concurrent requests no longer clobber a shared slot). The read side still surfaces a single primary request, so the SSE `/request-progress` contract and the waterfall UI are unchanged. See `src/observability/CLAUDE.md` for the full method list and the request-scoped-mutator rule.

1. `startRequest()` — creates RequestProgress keyed by a fresh requestId
2. `toolStart(requestId, …)`/`toolEnd(requestId, …)` — records tool calls with timing
3. `completeRequest(requestId, …)` — marks done, auto-clears that request after 30s
4. Phases: `idle`, `receiving`, `transcribing`, `building_prompt`, `calling_claude`, `saving_response`, `sending_telegram`, `sending_slack`, `synthesizing_voice`, `running_task`, `checking_goals`, `running_watcher`

### Activity Log (`src/observability/activity-log.ts`)

- In-memory ring buffer (max 500 events) + fire-and-forget DB persistence
- `loadFromDb()` called after DB init to hydrate on startup
- `stats` getter: messages today, avg response time, total cost

## MCP Debug Client (`src/ai/mcp-tool-caller.ts`)

- Supports both stdio (spawn process) and HTTP (StreamableHTTP) transports
- Connection pool keyed by `bot:server` — reuses existing connections
- Timeouts: 30s connect, 120s tool call
- Used by the MCP debug page (`tools-routes.ts`), Serena management, and the `openai-compat` connector

## Common Pitfalls

1. **SSE format**: Events must be `data: JSON\n\n` — missing double newline breaks the stream.
2. **Agent status waterfall is single-pane**: the data layer tracks all concurrent requests (per-`requestId` Map), but `/request-progress` emits only the primary (most-recently-started) request, so the overlay shows one at a time.
3. **Component pattern**: Always export all three (*Styles, *Html, *Script) even if empty — pages expect them.
4. **MCP connection cleanup**: Always call `disconnectServer()` or `disconnectAll()` — leaked stdio processes stay alive.
